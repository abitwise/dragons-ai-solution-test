---
phase: 03-game-loop-shop-integration
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - src/runner.ts
  - src/runner.test.ts
autonomous: true
requirements: [LOOP-02, LOOP-03]
must_haves:
  truths:
    - "The loop terminates via the max-turn safety cap (state.turn > MAX_TURN → reason TURN_CAP), proving a climbing turn counter cannot spin forever"
    - "The loop terminates via the no-progress guard (state.turn fails to advance for 3 consecutive iterations → reason NO_PROGRESS), proving a flat turn counter cannot spin forever"
    - "A permanently-empty board (chooseAd always null, shop buys nothing) rides into the no-progress guard and ends with NO_PROGRESS — one unified stall-termination, no separate empty-board reason"
    - "A thrown ApiClient error mid-game (BoundaryError / TransportError) propagates as a rejected promise carrying the ORIGINAL typed error — playGame does NOT catch or wrap it"
    - "A solve with success:false (lives > 0) and a buy with shoppingSuccess:false are NORMAL play — they do NOT reject the run; only THROWN errors terminate it"
  artifacts:
    - path: "src/runner.ts"
      provides: "the MAX_TURN cap check, the no-progress stall counter + guard, and the verified error pass-through (no try/catch wrapping)"
      exports: ["playGame"]
      contains: "MAX_TURN"
    - path: "src/runner.test.ts"
      provides: "offline tests for TURN_CAP, NO_PROGRESS, empty-board→NO_PROGRESS, typed-error rejection, and success:false-is-normal"
      contains: "rejects.toBeInstanceOf"
  key_links:
    - from: "src/runner.ts (cap check)"
      to: "END.TURN_CAP"
      via: "state.turn > MAX_TURN"
      pattern: "MAX_TURN"
    - from: "src/runner.ts (stall counter)"
      to: "END.NO_PROGRESS"
      via: "consecutive iterations with no turn advance >= NO_PROGRESS_LIMIT"
      pattern: "NO_PROGRESS_LIMIT"
    - from: "src/runner.test.ts (error case)"
      to: "src/api.ts (BoundaryError / TransportError)"
      via: "await expect(playGame(fake, logger)).rejects.toBeInstanceOf(BoundaryError)"
      pattern: "toBeInstanceOf\\((BoundaryError|TransportError)\\)"
---

<objective>
Extend `src/runner.ts` test-first with the two termination guards and verify the error-propagation
behavior, completing Phase 3. This plan delivers: the max-turn safety cap (`state.turn > MAX_TURN`
→ `END.TURN_CAP`, D-05), the no-progress guard (turn counter stalls for `NO_PROGRESS_LIMIT = 3`
consecutive iterations → `END.NO_PROGRESS`, D-06/D-07), the empty-board-rides-into-no-progress
unification (D-14), and the proof that a thrown ApiClient error propagates raw while solve/buy
"failure" bodies are normal play (D-11/D-12/D-13). Together with plan 03-01 this proves the loop
can NEVER run forever and ends cleanly on a mid-game API error (LOOP-02, the remainder of LOOP-03).

Purpose: The dual guards are the safety contract of the whole bot — "it can never run forever."
TDD here is essential: the only way to prove non-termination is impossible is to script both the
climbing-turn and flat-turn scenarios and assert each trips its guard. The error case is verified
by a controlled rejection (D-12), the offline equivalent of "ends cleanly" before Phase 4's CLI
catch.

Output: A complete, fully-tested `playGame` whose loop provably terminates in all three scenarios
(GAME_OVER, TURN_CAP, NO_PROGRESS) and whose mid-game-error path is a clean typed rejection.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-loop-shop-integration/03-CONTEXT.md
@.planning/phases/03-game-loop-shop-integration/03-PATTERNS.md
@.planning/phases/03-game-loop-shop-integration/03-01-SUMMARY.md
@src/types.ts
@src/api.ts
@src/strategy.ts
@src/fake-api-client.ts
@src/api.test.ts
</context>

<feature>
  <name>playGame termination guards (TURN_CAP + NO_PROGRESS) and verified error pass-through</name>
  <files>src/runner.ts, src/runner.test.ts</files>
  <behavior>
    Offline FakeApiClient-driven cases (RED first):
    - Max-turn cap (D-05): script solves whose `turn` keeps climbing past MAX_TURN while lives stay > 0
      → playGame resolves to a GameReport with reason END.TURN_CAP.
    - No-progress guard (D-06): script a scenario where `state.turn` does NOT advance for 3 consecutive
      iterations (e.g. empty boards / all-unaffordable shop so nothing turn-consuming happens) → resolves
      with reason END.NO_PROGRESS. Also assert the stall counter RESETS when turn advances (a script that
      stalls twice, then advances, then stalls again must NOT trip at the 4th total stall).
    - Empty board → NO_PROGRESS (D-14): chooseAd returns null every iteration AND the shop buys nothing
      → no turn-consuming action → state.turn flat → ends with NO_PROGRESS after exactly 3 stalls (no
      separate empty-board reason constant).
    - Typed-error rejection (D-11/D-12): a FakeApiClient method scripted to throw a BoundaryError mid-game
      → `await expect(playGame(fake, logger)).rejects.toBeInstanceOf(BoundaryError)`. A second variant with
      TransportError → rejects.toBeInstanceOf(TransportError). The runner adds NO try/catch wrap.
    - Failure-body-is-normal (D-13): a solve with success:false but lives > 0, and a buy with
      shoppingSuccess:false, must NOT reject — the run continues to a later lives:0 GAME_OVER.
  </behavior>
  <implementation>
    Add the cap check, stall counter + guard, and (verify, do not add) error pass-through to the
    existing src/runner.ts AFTER the tests are RED. Concrete target state below.
  </implementation>
</feature>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing tests for the turn cap, no-progress guard, empty-board termination, and typed-error rejection</name>
  <files>src/runner.test.ts</files>
  <read_first>
    - src/runner.test.ts (the existing suite + fixture builders from plan 03-01 — reuse baseState/solveFixture/adFixture/silent-logger; add the new describe block, do not duplicate builders)
    - src/runner.ts (the playGame loop, END/MAX_TURN/NO_PROGRESS_LIMIT constants declared by 03-01 — read the exact END reason strings to assert against, and confirm MAX_TURN's value so the cap test can script turn > MAX_TURN)
    - src/api.test.ts line 20 (the error-class import `import { BoundaryError, TransportError } from "./api.js"`) and the `.rejects.toBeInstanceOf(...)` idiom at lines 226 / 254 / 262 — copy this idiom EXACTLY
    - src/api.ts lines 52-77 (the `TransportError` and `BoundaryError` classes the rejection cases assert on — both `extends Error` with `readonly status?`)
    - src/fake-api-client.ts lines 94-116 (a FUNCTION source can `throw` to drive the error case; fail-loud on queue exhaustion means scripts must supply exactly the responses each scenario consumes)
    - .planning/phases/03-game-loop-shop-integration/03-CONTEXT.md (D-05 turn cap, D-06 no-progress = turn counter stalls 3 consecutive iterations with reset-on-advance, D-07 dual guards guarantee termination, D-11 fatal errors propagate raw / playGame does not catch, D-12 ends-cleanly = controlled rejection, D-13 solve/buy failure bodies are normal play, D-14 empty board rides into no-progress)
  </read_first>
  <action>
    Extend `src/runner.test.ts` (do NOT create a new file). Add `import { BoundaryError, TransportError } from "./api.js"`
    alongside the existing imports. Reuse the existing fixture builders and silent logger. Add a new
    `describe("playGame termination & errors", ...)` block with these failing tests:

    1. "trips the max-turn cap when turn climbs past MAX_TURN (D-05)": script a `solve` FUNCTION source that
       returns `solveFixture({ success: true, lives: 3, turn: <call-count-based, climbing past MAX_TURN> })`,
       a `getShop` returning `[]` (no buys), and a `getMessages` returning a solvable board each call. Assert the
       resolved report's `reason` equals the END.TURN_CAP string (`"stopped: max-turn cap reached"`). Because lives
       never reach 0, only the cap can stop it — proving the climbing-turn branch terminates.
    2. "trips the no-progress guard after 3 consecutive stalls (D-06)": script `getShop` returning `[]` (buys
       nothing) and `getMessages` returning an empty board `[]` every call, with no solve scripted at all. Since
       chooseAd returns null, no solve fires, state.turn stays flat → assert the resolved report's `reason` equals
       END.NO_PROGRESS (`"stopped: no-progress guard tripped"`). Confirm the run made exactly 3 stall iterations
       (e.g. assert `fake.calls.filter(c => c.method === "getMessages").length === 3`).
    3. "resets the stall counter when turn advances (D-06)": script a sequence that stalls twice (empty board),
       then advances once (a solvable ad + a solve that increments turn but keeps lives > 0), then stalls again —
       assert it does NOT terminate at the 4th total stall but only after 3 CONSECUTIVE stalls following the advance
       (drive it to NO_PROGRESS at the right iteration count). This proves the reset semantics.
    4. "empty board rides into the no-progress guard (D-14)": effectively the same shape as case 2 but framed as the
       empty-board scenario — assert the reason is END.NO_PROGRESS and that there is NO separate empty-board reason
       string (only the three END constants exist; grep-style assertion that the report.reason is one of the three).
    5. "propagates a thrown BoundaryError as a rejected promise without wrapping (D-11/D-12)": script a method (e.g.
       `getMessages: () => { throw new BoundaryError("boom", 400); }`) after a valid startGame/getShop; assert
       `await expect(playGame(fake, logger)).rejects.toBeInstanceOf(BoundaryError)`.
    6. "propagates a thrown TransportError likewise (D-11/D-12)": same shape with `throw new TransportError("net", 503)`;
       assert `rejects.toBeInstanceOf(TransportError)`.
    7. "a solve success:false (lives > 0) is normal play and does NOT reject (D-13)": script a solve returning
       `{ success: false, lives: 2, turn: 1, ... }` (lives still > 0) followed by a lives:0 solve; assert playGame
       RESOLVES to a GAME_OVER report (the success:false body did not reject the run).
    8. "a buy shoppingSuccess:false does NOT reject (D-13)": script a buy returning `{ shoppingSuccess: false, ... }`
       then drive to a lives:0 solve; assert playGame RESOLVES to a GAME_OVER report.

    Do NOT modify src/runner.ts in this task. Run the suite; the new tests MUST fail (the guards are not yet wired,
    so the climbing-turn and flat-turn scripts will instead exhaust the fake's queues and reject with a FakeApiClient
    fail-loud error rather than returning TURN_CAP/NO_PROGRESS — that is the correct RED signal). Commit RED.
  </action>
  <verify>
    <automated>npx vitest run src/runner.test.ts 2>&1 | grep -qiE "(failed|fail)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/runner.test.ts now imports `{ BoundaryError, TransportError }` from "./api.js"
    - A new describe block adds at least 8 `it(...)` cases covering: TURN_CAP, NO_PROGRESS (3 stalls), stall-counter reset, empty-board→NO_PROGRESS, BoundaryError rejection, TransportError rejection, solve-success:false-is-normal, buy-shoppingSuccess:false-is-normal
    - At least two cases use `.rejects.toBeInstanceOf(BoundaryError)` / `.rejects.toBeInstanceOf(TransportError)` (the api.test.ts idiom)
    - `npx vitest run src/runner.test.ts` exits NON-zero (RED) — the guard cases fail because the cap/no-progress checks are not yet wired
    - The plan-03-01 tests in the same file still pass (the new RED cases are additive; do not break the happy path)
    - A commit exists matching `test(03-02): ...` (RED gate)
  </acceptance_criteria>
  <done>The RED test suite additions are written and committed and fail because the termination guards are not yet wired into playGame.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN + REFACTOR): Wire the turn cap and no-progress guard into playGame; confirm error pass-through</name>
  <files>src/runner.ts, src/runner.test.ts</files>
  <read_first>
    - src/runner.ts (the existing playGame loop and the MAX_TURN / NO_PROGRESS_LIMIT / END constants declared by 03-01 — wire the checks into the existing loop, do not rewrite the shop drain / fresh-ads logic)
    - src/runner.test.ts (the RED tests from this plan's Task 1 — implement exactly to satisfy them)
    - .planning/phases/03-game-loop-shop-integration/03-PATTERNS.md lines 119-130 (the termination-guards pattern: track previous state.turn, stall counter with reset-on-advance, cap check; and the error pass-through pattern — NO try/catch wrap around API calls)
    - .planning/phases/03-game-loop-shop-integration/03-CONTEXT.md (D-05, D-06, D-07, D-10 no API_ERROR reason, D-11 raw propagation, D-14 unified stall termination — the order of guard checks vs the lives check is at planner/executor discretion as long as the loop provably cannot spin)
    - src/api.ts lines 52-77 (confirm the runner does NOT import or reference these error classes in src/runner.ts — they propagate untouched; only the TEST imports them)
  </read_first>
  <action>
    Modify `src/runner.ts` playGame loop ONLY (keep the 03-01 shop drain + fresh-ads-before-solve + state-threading
    intact). Add the two guards using the already-declared `MAX_TURN`, `NO_PROGRESS_LIMIT`, and `END` constants:

    - NO-PROGRESS STALL COUNTER (D-06/D-14): before the loop, `let prevTurn = state.turn; let stalls = 0;`. At the
      TOP of each outer iteration, compare the current `state.turn` to `prevTurn` from the END of the previous
      iteration: if `state.turn === prevTurn` increment `stalls`, else reset `stalls = 0` and update `prevTurn`.
      (Implement the compare/update so a turn-advance resets the counter and a flat turn accumulates — match the
      "reset the stall counter whenever turn advances" semantics, PATTERNS.md lines 119-122.) When
      `stalls >= NO_PROGRESS_LIMIT` (3), `return { score: state.score, turns: state.turn, reason: END.NO_PROGRESS }`.
    - MAX-TURN CAP (D-05): when `state.turn > MAX_TURN`,
      `return { score: state.score, turns: state.turn, reason: END.TURN_CAP }`.
    - Place these guard checks so the loop provably cannot spin (D-07): both must be reachable every iteration
      regardless of whether the shop or solve consumed a turn. The existing `lives === 0` exit yields
      `END.GAME_OVER`. The exact ordering of (lives check / cap check / no-progress check) is at executor discretion
      as long as all three are checked each iteration and the loop terminates in every scenario.
    - ERROR PASS-THROUGH (D-11): add NO try/catch around the api calls — a thrown TransportError/BoundaryError
      already propagates as a rejected promise (the api client retried reads / never retried solve+buy in Phase 1).
      You MAY add an optional in-context `logger.error(...)` ONLY if it does not swallow or re-throw a new type; the
      simplest correct implementation adds nothing and lets the error propagate untouched. Do NOT add an
      `API_ERROR` reason (D-10) — error exits produce NO GameReport.
    - The runner must NOT import `BoundaryError`/`TransportError` (only the test does) — confirm src/runner.ts has no
      such import.

    Run the full suite; iterate to GREEN. REFACTOR (only if obvious and tests stay green): e.g. extract a small
    `shouldStop(state, stalls)` predicate returning the END reason or null, keeping the loop readable; commit only if
    code actually changed.
  </action>
  <verify>
    <automated>npx vitest run src/runner.test.ts && npx vitest run && npx tsc --noEmit && npx biome check src/runner.ts src/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/runner.ts` references `MAX_TURN` in a cap check returning `reason: END.TURN_CAP`: `grep -cE "END\.TURN_CAP" src/runner.ts` returns at least 1
    - `src/runner.ts` references `NO_PROGRESS_LIMIT` in a stall guard returning `reason: END.NO_PROGRESS`: `grep -cE "END\.NO_PROGRESS" src/runner.ts` returns at least 1
    - `src/runner.ts` contains NO try/catch wrapping API calls: `grep -c 'catch' src/runner.ts` returns 0
    - `src/runner.ts` does NOT import the error classes: `grep -cE "(BoundaryError|TransportError)" src/runner.ts` returns 0
    - `src/runner.ts` defines no `API_ERROR` reason — the END object still has exactly three keys (GAME_OVER, TURN_CAP, NO_PROGRESS)
    - `npx vitest run src/runner.test.ts` exits 0 (GREEN) — TURN_CAP, NO_PROGRESS, reset, empty-board, both typed-error rejections, and both failure-body-is-normal cases pass
    - `npx vitest run` exits 0 — the FULL suite (Phases 1-2 + 03-01 + 03-02) passes with zero live network
    - `npx tsc --noEmit` exits 0 and `npx biome check src/runner.ts src/runner.test.ts` reports no errors
    - A commit exists matching `feat(03-02): ...` (GREEN gate); a `refactor(03-02): ...` commit only if cleanup was done
  </acceptance_criteria>
  <done>Both guards are wired and verified: the climbing-turn script ends with TURN_CAP, the flat-turn/empty-board script ends with NO_PROGRESS after 3 stalls (resetting on advance), a thrown error rejects with its original typed class, and solve/buy failure bodies do not reject. The full suite, type-check, and lint are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response `turn`/`lives` fields → loop control | The loop's termination depends on API-provided `state.turn` (cap + no-progress) and `state.lives` (game-over). This is the primary trust boundary this plan hardens. |
| thrown ApiClient error → playGame caller | A thrown `TransportError`/`BoundaryError` crosses out of playGame as a rejected promise; the runner deliberately adds no error boundary (the boundary is index.ts in Phase 4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | playGame loop with a CLIMBING but never-game-over turn counter | mitigate | The max-turn cap (D-05): `state.turn > MAX_TURN` → `END.TURN_CAP`, a generous backstop (2000) well above any real game. Verified by the climbing-turn test which can only stop via the cap (lives never reach 0). |
| T-03-06 | Denial of Service | playGame loop with a FLAT / never-advancing turn counter (hostile API, permanently-empty board) | mitigate | The no-progress guard (D-06/D-07): the stall counter trips after `NO_PROGRESS_LIMIT = 3` consecutive non-advancing iterations → `END.NO_PROGRESS`. This is the essential complement the turn-cap alone cannot catch (a flat turn never reaches MAX_TURN). Verified by the flat-turn + empty-board (D-14) tests; the reset-on-advance test proves the counter cannot falsely trip during normal play. |
| T-03-07 | Tampering / Repudiation | API error swallowed or remapped, hiding a real failure | mitigate | D-11: the runner adds NO try/catch wrap and NO new error type — the ORIGINAL typed error propagates verbatim, so the Phase-4 CLI sees the true failure (status, cause) for an accurate exit code. There is deliberately no `API_ERROR` GameReport reason (D-10) that could mask a crash as a normal end. Verified by `rejects.toBeInstanceOf(BoundaryError/TransportError)`. |
| T-03-08 | Spoofing | a solve/buy "failure" body misread as a fatal error (or vice-versa) | mitigate | D-13: `success:false` / `shoppingSuccess:false` are NORMAL play (folded, loop continues); only THROWN errors terminate. The two "failure-body-is-normal" tests prove a failed ad/buy does not reject, while the typed-error tests prove a thrown error does — the two paths cannot be confused. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs NO new packages — it extends existing in-repo modules and uses already-installed Vitest. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/runner.test.ts` exits 0 — TURN_CAP, NO_PROGRESS (with reset), empty-board→NO_PROGRESS, both typed-error rejections, and both failure-body-is-normal cases pass.
- `npx vitest run` exits 0 — the FULL suite (all phases) passes with ZERO live network calls.
- `npx tsc --noEmit` exits 0; `npx biome check .` reports no errors.
- `grep -c 'catch' src/runner.ts` is 0 and `grep -cE "(BoundaryError|TransportError)" src/runner.ts` is 0 — the runner adds no error boundary and imports no error class (D-11).
- The END object has exactly three reason keys (no API_ERROR — D-10).
- Git log shows the RED `test(03-02)` commit preceding the GREEN `feat(03-02)` commit (TDD gate sequence).
</verification>

<success_criteria>
- ROADMAP Criterion #2: the loop terminates via the max-turn cap and via the no-progress guard in their respective scenarios — proven offline, so it can never spin forever (LOOP-02).
- ROADMAP Criterion #3 (completion): the empty-board fallback (chooseAd null) rides into the unified no-progress termination (D-14), no separate path.
- ROADMAP Criterion #4: a mid-game ApiClient error ends the run cleanly as a controlled typed rejection (D-12), not a crash, verified offline against the fake with no live network; solve/buy failure bodies are normal play (D-13).
- LOOP-02: max-turn cap + no-progress guard guarantee termination (D-05/D-06/D-07).
- The dual guards together make non-termination impossible (D-07): turn either climbs (→ TURN_CAP) or stalls (→ NO_PROGRESS).
</success_criteria>

<output>
Create `.planning/phases/03-game-loop-shop-integration/03-02-SUMMARY.md` when done, recording:
- RED: which guard/error tests were written and why they failed (guards unwired).
- GREEN: the cap + stall-counter implementation and confirmation the error path needed no code (pure pass-through).
- REFACTOR: any cleanup (e.g. extracted shouldStop predicate) or "none".
- Commits: the `test(03-02)` / `feat(03-02)` (/ optional `refactor(03-02)`) hashes.
- Confirm Phase 3 is feature-complete: GAME_OVER / TURN_CAP / NO_PROGRESS all reachable and tested; error path is a clean typed rejection.
</output>
