---
phase: 01-foundation-types-api-client-test-seam
plan: 04
type: tdd
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/api.ts
  - src/api.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
user_setup: []

must_haves:
  truths:
    - "HttpApiClient is the ONLY module that calls fetch; it implements ApiClient and returns typed models for startGame/getMessages/solve/getShop/buy against the live API shape"
    - "A string reward in a messages response is coerced to a number at the boundary before it enters the Ad model (API-06)"
    - "A non-JSON (HTML) error body on a non-2xx response is handled without an unhandled crash — surfaced as a typed boundary error (API-06, PITFALLS #5)"
    - "The body's `success` boolean (NOT the HTTP status) is the source of truth for a solve: an HTTP-200 solve with `success: false` is returned as a FAILED SolveResult, no error thrown (API-06, PITFALLS #5, Locked Carry-Forward)"
    - "An adId containing '/', '+', or '=' is URL-encoded so /solve does not 400 (API-06, PITFALLS #2)"
    - "Idempotent reads (GET messages, GET shop, POST game/start) retry ~3x with bounded backoff on 5xx/network errors; POST /solve and POST /buy are NEVER auto-retried (D-04/D-05)"
    - "A zod validation failure is terminal — it bypasses retry, is wrapped as a typed boundary error, and bubbles to the caller (D-06)"
    - "The base URL defaults to the non-www host https://dragonsofmugloar.com/api/v2 and is overridable via MUGLOAR_BASE_URL (env-only, not from any API response)"
    - "getMessages decodes encrypted ads via decodeAd before returning, so callers never see an encoded adId/probability for a handled scheme (API-05)"
  artifacts:
    - path: "src/api.ts"
      provides: "zod schemas per endpoint (raw wire shape + light coercion), boundary error taxonomy (retryable transport error vs terminal boundary/parse error), fetch+retry transport, and HttpApiClient implementing ApiClient"
      exports: ["HttpApiClient"]
      contains: "implements ApiClient"
      min_lines: 80
    - path: "src/api.test.ts"
      provides: "RED→GREEN tests stubbing global fetch: reward coercion, HTML-error-body tolerance, success-field-is-source-of-truth (200 + success:false = failed solve), encodeURIComponent of a /-containing adId, retry-on-5xx for reads, no-retry for solve/buy, ZodError-bypasses-retry, base-URL default + MUGLOAR_BASE_URL override, getMessages decode integration"
      contains: "HttpApiClient"
  key_links:
    - from: "src/api.ts"
      to: "src/decode.ts"
      via: "getMessages maps each validated ad through decodeAd"
      pattern: "decodeAd"
    - from: "src/api.ts"
      to: "src/types.ts"
      via: "implements ApiClient; returns GameState/Ad/SolveResult/ShopItem"
      pattern: "implements ApiClient"
    - from: "HttpApiClient solve/buy"
      to: "URL path segments"
      via: "encodeURIComponent on gameId, adId, itemId"
      pattern: "encodeURIComponent"
    - from: "HttpApiClient"
      to: "global fetch"
      via: "the only fetch caller, wrapped with bounded retry for idempotent reads"
      pattern: "fetch\\("
---

<objective>
Implement `HttpApiClient` — the ONLY module that calls `fetch` — fully test-first (RED → GREEN → REFACTOR), with global `fetch` stubbed (no live network, per the locked carry-forward and ROADMAP success criterion 5). This plan owns `src/api.ts` end-to-end: the zod schemas (one per endpoint, co-located, raw wire shape + light coercion per D-01/D-02), the boundary error taxonomy (retryable transport error vs terminal boundary/parse error per D-06), the fetch-with-retry transport (D-04/D-05), `encodeURIComponent` on every path segment (PITFALLS #2), the non-www base URL with `MUGLOAR_BASE_URL` override, tolerant HTML-error-body handling plus `success`-as-body-field solve semantics (PITFALLS #5), and the five `ApiClient` methods wired through `decodeAd` (Plan 02). The client is intentionally ONE cohesive unit (a single plan) per the keep-it-simple brief (CLAUDE.md) — minimal layers, no over-engineering; it is not split into sub-plans.

It implements the real client behind the `ApiClient` interface so production (Phase 4) can solve, buy, fetch, and start a game against the live API while never crashing on its verified quirks. Implements API-01..API-06.

Purpose: A correct, robust, exhaustively-tested real client at the external-API edge, keeping all HTTP/retry/decode/coercion concerns in ONE place so strategy and runner never learn about fetch.
Output: `src/api.ts` (schemas + errors + transport + `HttpApiClient implements ApiClient`) and `src/api.test.ts`, all green, zero live network.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@src/types.ts
@src/decode.ts
@CLAUDE.md
</context>

<feature>
  <name>HttpApiClient — the only fetch caller: zod-validated, retry-at-the-edge, decode-integrated client</name>
  <files>src/api.ts, src/api.test.ts</files>
  <behavior>
    Build test-first. Write `src/api.test.ts` cases (RED), then implement `src/api.ts` until green (GREEN), then refactor (REFACTOR). Tests stub global `fetch` (e.g. `vi.spyOn(globalThis, "fetch")` or assign a `vi.fn()` to `globalThis.fetch`) — NO nock/msw, NO live network. The behavior to drive out:

    SCHEMAS & COERCION (D-01/D-02, API-06):
    - The messages schema validates the raw wire ad shape and COERCES a string `reward` (e.g. `"100"`) to the number `100` via `z.coerce.number()` BEFORE it enters the Ad model. Test: a messages response with `reward: "100"` yields `Ad.reward === 100` (number).
    - `encrypted` is validated as an OPTIONAL number (not a strict 1|2 union) so an unknown scheme passes validation rather than throwing (D-02).
    - start/solve/shop/buy each have their own co-located schema matching FEATURES.md field references (lines 31-67), including the solve/buy asymmetry (solve has score not level; buy has level not score).

    DECODE INTEGRATION (API-05):
    - `getMessages` maps each schema-validated ad through `decodeAd` (from `./decode`) BEFORE returning, so a handled encrypted ad arrives decoded with its flag cleared, and an unhandled one stays flagged for strategy. Decode is a SEPARATE step AFTER zod validation (D-03), not a `.transform`. Test: a messages response containing an `encrypted:1` ad returns it Base64-decoded.

    URL ENCODING (API-06, PITFALLS #2):
    - solve builds the path `/{gameId}/solve/{adId}` with `encodeURIComponent` on gameId AND adId; buy encodes gameId AND itemId; messages/shop encode gameId. Test: `solve("g1", "a/b+c=d")` causes a fetch URL whose adId segment is the encodeURIComponent output (contains `%2F`, no raw `/` in the id segment) — assert on the URL string passed to the stubbed fetch.

    RETRY POLICY (D-04/D-05, API-06):
    - Idempotent reads — getMessages, getShop, AND startGame — retry ~3 attempts with bounded backoff on a 5xx response OR a thrown network/transport error. Test: fetch returns 500 twice then 200 → getMessages succeeds after retries; a getMessages that gets 500 on every attempt eventually throws the typed transport error.
    - solve and buy are NEVER auto-retried. Test: fetch returns 500 for solve → solve throws/surfaces immediately with NO second fetch call (assert fetch call count === 1). Same for buy.
    - Make backoff fast/deterministic in tests (e.g. inject the delay function or use fake timers) so the suite stays fast and offline. Each `await delay` must be bounded — no unbounded retry loop (T-01-07).

    ERROR TAXONOMY & HTML BODIES (D-06, API-06, PITFALLS #5):
    - Define two distinct error types (Claude's discretion on exact names, e.g. `TransportError` for retryable 5xx/network and `BoundaryError`/`ParseError` for terminal validation/parse failures). A non-2xx response whose body is HTML (not JSON) does NOT crash with an unhandled JSON.parse error — it is read as text and surfaced as a typed error. Test: a 400 response with an HTML body (`<html>...`) makes solve reject with the typed boundary error, not a raw SyntaxError.
    - A ZodError (schema drift — body validates wrong) is TERMINAL: it BYPASSES retry, is wrapped as the typed boundary/parse error, and bubbles to the caller (D-06). Test: getMessages whose 200 body fails schema validation throws the boundary error AFTER exactly ONE fetch call (no retry on a parse failure) — assert fetch call count === 1.

    SOLVE SUCCESS IS A BODY FIELD, NOT HTTP STATUS (API-06, PITFALLS #5, Locked Carry-Forward):
    - The body's `success` boolean — NOT the HTTP status — is the source of truth for whether a solve succeeded. A solve response with HTTP 200 but a body of `{ success: false, lives: <n>, gold: <g>, score: <s>, highScore: <h>, turn: <t>, message: <string> }` is a VALID, well-formed response that simply reports a FAILED attempt; it must be returned as a normal `SolveResult` (NOT treated as an error). Test: stub fetch to return HTTP 200 with body `{ success: false, lives, gold, score, highScore, turn, message }` for `solve` → assert the resolved `SolveResult.success === false`, that the typed model still parses cleanly (it is a valid response, just unsuccessful — every state field is present and typed), and that NO error is thrown / the promise resolves rather than rejects. This proves `success` (the body field) — not HTTP-2xx — is the source of truth for a solve. (Conversely, a 200 with `success: true` resolves with `SolveResult.success === true`; the two cases differ ONLY by the body field, not the status code.)

    BASE URL (Claude's discretion + locked: non-www):
    - The base URL defaults to `https://dragonsofmugloar.com/api/v2` (the NON-www host — www returned nginx 404s live) and is overridable via the `MUGLOAR_BASE_URL` env var, read ONCE from env at construction — never from an API response (SSRF guard, T-01-08). Test: with no env override the constructed URL starts with the non-www base; with `MUGLOAR_BASE_URL` set the override is used.

    The five methods return typed models: startGame→GameState, getMessages→Ad[] (decoded), solve→SolveResult, getShop→ShopItem[], buy→GameState (merged buy fields). A request timeout (e.g. via AbortController/AbortSignal.timeout) bounds a hung connection so the bot can't stall forever (T-01-07).
  </behavior>
  <implementation>
    Implement in `src/api.ts`. Import the `ApiClient`, `GameState`, `Ad`, `ShopItem`, `SolveResult` types from `./types` and `decodeAd` from `./decode`. Co-locate one zod schema per endpoint (D-01) at module scope; use `z.coerce.number()` for `reward`; type `encrypted` as `z.number().optional()` (D-02). The solve schema validates `success` as a boolean and the full result state (lives/gold/score/highScore/turn/message) so that a `success: false` body is a SUCCESSFUL parse that yields a SolveResult whose `success` is false — never inferred from the HTTP status. Define a small error taxonomy: a retryable transport error class and a terminal boundary/parse error class. Write a single private `request<T>(method, path, schema, { retry })` helper that is the ONLY place `fetch` is called: it builds the full URL from the base URL + path (path already built with `encodeURIComponent` segments by the caller), applies an AbortSignal timeout, and on a 5xx-or-network failure retries up to ~3 attempts with bounded backoff WHEN `retry` is true (reads) and NOT at all when false (solve/buy); on non-2xx it reads the body as text and throws the terminal boundary error (handles HTML bodies — never assume JSON on non-2xx); on 2xx it parses JSON and runs the zod schema, wrapping any ZodError as the terminal boundary error so it bypasses retry (D-06). Because `success` lives in a 2xx body, a failed solve takes the normal 2xx parse path and resolves — it does NOT enter the non-2xx error branch. Export `HttpApiClient` as a class or factory that `implements ApiClient`; its constructor reads `MUGLOAR_BASE_URL` from `process.env` once (default the non-www base). `getMessages` validates with the messages schema then maps each ad through `decodeAd` before returning. `startGame`/`getMessages`/`getShop` call `request` with `retry: true`; `solve`/`buy` call with `retry: false`. Keep ALL fetch usage inside this module — no other file ever calls fetch. Inject or parameterize the backoff delay so tests run fast.
  </implementation>
</feature>

<verification>
- `npx vitest run src/api.test.ts` exits 0 with every documented case green and ZERO live network calls (fetch is stubbed)
- `npx tsc --noEmit` exits 0 (HttpApiClient is a valid ApiClient implementation)
- `grep -c 'fetch(' src/api.ts` accounts for fetch only inside the single request helper; `grep -L 'fetch(' src/decode.ts src/fake-api-client.ts src/types.ts` confirms no other module calls fetch
- Reward coercion, HTML-error tolerance, success-field-is-source-of-truth (HTTP 200 + body `success: false` resolves as a failed `SolveResult` with `success === false` and NO error thrown), encodeURIComponent, read-retry, solve/buy-no-retry, ZodError-bypasses-retry, base-URL default + override, and getMessages-decode each have a passing assertion
</verification>

<success_criteria>
- Failing tests written and committed first (RED): `test(01-04): add failing tests for HttpApiClient (coercion, retry, encode, errors)`
- Implementation passes (GREEN): `feat(01-04): implement HttpApiClient with zod boundary, retry-at-the-edge, decode integration`
- Refactor if needed (REFACTOR): `refactor(01-04): clean up request helper / error taxonomy`
- ROADMAP success criterion 2: HttpApiClient can start/messages/solve/shop/buy against the live API shape, returning typed models
- ROADMAP success criterion 3 (completed): encrypted ad decoded across all three fields AND /-containing adId URL-encoded so /solve does not 400
- ROADMAP success criterion 4: transient failure retried with bounded backoff; non-JSON (HTML) error body handled without an unhandled crash; string reward coerced to number at the boundary
- The body `success` field (not HTTP status) is the source of truth for a solve: a 200 + `success: false` resolves as a failed SolveResult without throwing (PITFALLS #5, Locked Carry-Forward)
- API-01..API-06 all satisfied in this client
- D-01/D-02/D-03/D-04/D-05/D-06 honored
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HttpApiClient → Mugloar HTTP API | Outbound calls to one known third-party API; this is the only network egress in the codebase |
| Mugloar API response → app | The response body is UNTRUSTED — may be HTML/non-JSON, malformed, oversized, or carry attacker-influenceable ad strings |
| process.env → base URL | MUGLOAR_BASE_URL is operator-controlled config, read once at construction; never derived from an API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | retry loop + hung connection (resource exhaustion) | mitigate | Bounded retry (~3 attempts) with bounded backoff — NO unbounded loop; per-request AbortSignal timeout so a hung connection cannot stall the bot forever; solve/buy do not retry at all |
| T-01-08 | SSRF / Spoofing | base-URL trust | mitigate | Base URL defaults to the non-www host and is overridable ONLY via the MUGLOAR_BASE_URL env var read once at construction — never taken from an API response or other runtime/attacker-controllable source |
| T-01-09 | Tampering / DoS | untrusted-response handling (HTML/non-JSON/malformed body) | mitigate | Non-2xx bodies read as text and wrapped as a typed boundary error (never assume JSON on non-2xx — PITFALLS #5); ZodError on a 2xx body is terminal and wrapped, so schema drift surfaces cleanly instead of crashing; decoded ad text is treated as data only (decode is delegated to the already-hardened decodeAd) |
| T-01-10 | Tampering | adId/itemId/gameId path injection | mitigate | encodeURIComponent on every path segment so a `/`/`+`/`=` in an id cannot alter the route (PITFALLS #2) |
| T-01-SC | Tampering | zod runtime dependency at the boundary | accept | zod 4.x is the one approved runtime boundary dep (D-01), mainstream and high-download; legitimacy confirmed in Plan 01's install checkpoint; no new package installs occur in this plan |
</threat_model>

<output>
After completion, create `.planning/phases/01-foundation-types-api-client-test-seam/01-04-SUMMARY.md`.
</output>
