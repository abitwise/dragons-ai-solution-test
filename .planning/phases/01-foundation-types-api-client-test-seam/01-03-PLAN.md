---
phase: 01-foundation-types-api-client-test-seam
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/fake-api-client.ts
  - src/fake-api-client.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
user_setup: []

must_haves:
  truths:
    - "FakeApiClient implements the ApiClient interface, so Phase 2/3 tests wire it instead of HttpApiClient and run with zero network calls"
    - "Each method (startGame/getMessages/solve/getShop/buy) returns scripted responses from a per-method queue supplied at construction — no game logic lives inside the double"
    - "A test can script a final lives:0 solve result to drive Phase 3's play-to-game-over scenario"
    - "Calling a method whose queue is exhausted fails loudly (throws a clear error) rather than returning undefined, so a mis-scripted test is obvious"
  artifacts:
    - path: "src/fake-api-client.ts"
      provides: "Scripted FakeApiClient implementing ApiClient with per-method response queues (D-07)"
      exports: ["FakeApiClient"]
      contains: "implements ApiClient"
    - path: "src/fake-api-client.test.ts"
      provides: "Tests proving the double returns scripted values in order, supports a lives:0 script, and throws on queue exhaustion"
      contains: "FakeApiClient"
  key_links:
    - from: "src/fake-api-client.ts"
      to: "src/types.ts"
      via: "implements ApiClient interface from types.ts"
      pattern: "implements ApiClient"
    - from: "FakeApiClient constructor"
      to: "per-method response queues"
      via: "constructor accepts scripted responses (queues or functions)"
      pattern: "constructor"
---

<objective>
Hand-write `FakeApiClient` — the scripted test double that implements the `ApiClient` interface so the ENTIRE downstream suite (Phases 2-3) runs offline with zero live network calls (ROADMAP success criterion 5). Per the locked carry-forward, NO HTTP-mocking library (no nock/msw) is used: the seam is the injected interface plus this hand-written double.

Per D-07, the double is SCRIPTED / PROGRAMMABLE, NOT a stateful game simulator. Its constructor takes per-method response queues (or functions); each test scripts exactly the responses it needs — including a final `lives: 0` solve result to drive Phase 3's "play to game-over" test. NO game logic lives inside the double (so the double itself never needs trusting or testing for game behavior). The included tests verify only the mechanical contract: methods dequeue in order, a lives:0 script is supportable, and an exhausted queue fails loudly.

Purpose: Provide the offline test seam every later phase depends on. This double — not HttpApiClient — is what runner.test.ts and any strategy integration tests wire.
Output: `src/fake-api-client.ts` (`FakeApiClient implements ApiClient`) and a small `src/fake-api-client.test.ts` proving its mechanical contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@src/types.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the scripted FakeApiClient implementing ApiClient</name>
  <files>src/fake-api-client.ts</files>
  <read_first>
    - src/types.ts (the ApiClient interface — the five method signatures this double must implement exactly: startGame, getMessages, solve, getShop, buy; plus GameState, Ad, ShopItem, SolveResult shapes)
    - .planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md (D-07: scripted/programmable double, per-method response queues, NO game logic inside; the double is consumed BY tests and never needs its own behavior trusted)
    - .planning/research/ARCHITECTURE.md (Pattern 2 lines 147-168 — the FakeApiClient sketch with scripted arrays; "no HTTP mocking library" guardrail)
    - CLAUDE.md (no nock/msw; manual DI; keep it simple — a small class or factory returning an object literal satisfying ApiClient)
  </read_first>
  <action>
    Create `src/fake-api-client.ts` exporting `FakeApiClient` that `implements ApiClient` from `./types`. The constructor accepts a script object with one optional per-method queue (or function) for each ApiClient method — e.g. fields `startGame`, `getMessages`, `solve`, `getShop`, `buy`, each an array of pre-built return values (`GameState[]`, `Ad[][]`, `SolveResult[]`, `ShopItem[][]`, `GameState[]` respectively) OR a function producing the value. Each method, when called, dequeues (shift) the next scripted value and returns it wrapped in `Promise.resolve`. If a queue is exhausted (or was never provided) when its method is called, THROW a clear error naming the method (e.g. "FakeApiClient: no scripted response for solve") rather than returning undefined — a mis-scripted test must fail loudly, not silently return garbage. Keep the double mechanical: NO probability math, NO state mutation modeling the game, NO decode logic — it just returns what the test handed it. It is test infrastructure; it has no game logic to be trusted, so it is plain (no TDD cycle). Optionally record call arguments (e.g. an array of `{ method, args }`) so a test can assert "solve was called with adId X" — keep this optional and simple. No fetch, no console, no network anywhere in this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^[[:space:]]*//' src/fake-api-client.ts | grep -q 'implements ApiClient'</automated>
  </verify>
  <acceptance_criteria>
    - `src/fake-api-client.ts` exports `FakeApiClient` and the class declaration reads `implements ApiClient` (source assertion)
    - All five ApiClient methods (startGame, getMessages, solve, getShop, buy) are implemented and return Promises
    - The constructor accepts per-method scripted queues/functions (D-07) — no game-simulation logic in the body
    - Calling a method with an exhausted/absent queue throws an Error whose message names the method
    - No `fetch`, no `import` of nock/msw, no network call anywhere in the file
    - `npx tsc --noEmit` exits 0 (the double satisfies the ApiClient interface — type-checked)
  </acceptance_criteria>
  <done>FakeApiClient compiles as a valid ApiClient implementation, returns scripted per-method responses in order, and throws loudly on an exhausted queue. No game logic, no network.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical contract tests for FakeApiClient (offline)</name>
  <files>src/fake-api-client.test.ts</files>
  <read_first>
    - src/fake-api-client.ts (the constructor script shape and method behavior just implemented)
    - src/types.ts (GameState, Ad, SolveResult shapes to build scripted values)
    - .planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md (D-07: the double drives Phase 3's lives:0 game-over scenario — prove a lives:0 SolveResult can be scripted and returned)
    - .planning/research/PITFALLS.md (TDD pitfalls table lines 213-223 — "Fake client that decrements lives / returns game-over, plus an iteration cap, so the loop provably terminates": prove the lives:0 script path here)
  </read_first>
  <action>
    Create `src/fake-api-client.test.ts` using Vitest. Cover the MECHANICAL contract only (the double has no game logic to test): (1) a FakeApiClient scripted with two getMessages results returns them in order across two calls; (2) a FakeApiClient scripted with a `SolveResult` whose `lives` is 0 returns exactly that on `solve(...)` — this is the game-over script Phase 3 relies on; (3) startGame/getShop/buy each return their scripted value; (4) calling a method whose queue is exhausted throws an Error naming that method; (5) (if call-recording was implemented) `solve(gameId, adId)` records the adId so a test can assert it. Build all scripted values as plain typed objects — zero network, zero real HTTP. Do NOT assert on log strings.
  </action>
  <verify>
    <automated>npx vitest run src/fake-api-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/fake-api-client.test.ts` exits 0 with all cases green
    - A test proves a scripted `lives: 0` SolveResult is returned by `solve` (game-over script supported — D-07 / Phase 3 dependency)
    - A test proves an exhausted queue throws an Error naming the method
    - A test proves queued responses are returned in FIFO order
    - Zero network calls occur (no fetch, no real HTTP) — the suite is fully offline
  </acceptance_criteria>
  <done>The FakeApiClient's mechanical contract is proven offline: FIFO scripted responses, a scriptable lives:0 game-over result, and loud failure on queue exhaustion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | FakeApiClient is in-memory test infrastructure; it performs no I/O, processes no untrusted input, and never runs in production (index.ts wires HttpApiClient, not this) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | FakeApiClient accidentally shipped to production composition root | accept | Out of this phase's scope (index.ts is Phase 4) and architecturally prevented: only index.ts constructs real deps and it will wire HttpApiClient; the fake lives beside tests and is imported only by test files. No runtime/security surface here (no fetch, no I/O, no untrusted input) |
| T-01-06 | Denial of Service | mis-scripted test causing silent undefined returns | mitigate | Exhausted queues throw a clearly-named error rather than returning undefined, so a faulty test fails loudly instead of producing misleading green/garbage behavior |
</threat_model>

<verification>
- `npx vitest run src/fake-api-client.test.ts` exits 0, fully offline
- `npx tsc --noEmit` exits 0 — FakeApiClient is a valid ApiClient implementation
- The double has no game logic, no fetch, no nock/msw
</verification>

<success_criteria>
- `FakeApiClient implements ApiClient` and returns scripted per-method responses (D-07)
- A scriptable `lives: 0` solve result is proven (drives Phase 3 game-over)
- Exhausted queues fail loudly
- ROADMAP success criterion 1 reinforced (the rest of the codebase depends on the ApiClient interface, with a working offline double) and criterion 5 (suite runs with zero live network calls) is provable from this point forward
</success_criteria>

<output>
Create `.planning/phases/01-foundation-types-api-client-test-seam/01-03-SUMMARY.md` when done.
</output>
