---
phase: 01-foundation-types-api-client-test-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - .gitignore
  - src/types.ts
autonomous: false
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
user_setup: []

must_haves:
  truths:
    - "Running `npx tsc --noEmit` exits 0 against an empty greenfield repo with only src/types.ts present"
    - "Running `npx vitest run` exits 0 (zero tests is a pass) AND emits a recognizable Vitest discovery line, proving the runner actually ran test discovery (not just a misconfigured no-op)"
    - "types.ts exports the ApiClient interface — the single seam every later phase depends on; no module here imports fetch or zod"
    - "The shared vocabulary (GameState, Ad, ShopItem, SolveResult, GameReport, Logger) exists so strategy.ts (Phase 2) and runner.ts (Phase 3) import types.ts, never api.ts"
  artifacts:
    - path: "package.json"
      provides: "ESM project manifest, engines node 24, scripts dev/start/test/typecheck/lint, pinned deps (zod 4, pino, pino-pretty 13, typescript ~5.9, tsx 4, vitest 4, @biomejs/biome 2, @tsconfig/node24, @types/node)"
      contains: "\"type\": \"module\""
    - path: "tsconfig.json"
      provides: "extends @tsconfig/node24, noEmit true, strict type-checking"
      contains: "@tsconfig/node24"
    - path: "biome.json"
      provides: "Biome lint+format config"
    - path: "src/types.ts"
      provides: "GameState, Ad, ShopItem, SolveResult, GameReport, ApiClient, Logger declarations — no logic"
      contains: "interface ApiClient"
      min_lines: 40
  key_links:
    - from: "src/types.ts"
      to: "ApiClient interface"
      via: "exported interface with startGame/getMessages/solve/getShop/buy methods"
      pattern: "export interface ApiClient"
    - from: "package.json"
      to: "vitest + tsc + biome"
      via: "scripts block"
      pattern: "\"test\"|\"typecheck\"|\"lint\""
---

<objective>
Bootstrap the greenfield TypeScript project (no package.json, tsconfig, or src/ exist yet) and define `src/types.ts` — the leaf of the dependency graph that every other module imports. This plan unblocks the entire phase: nothing else can compile or run until the manifest, tsconfig, test runner, and shared type vocabulary exist.

This plan delivers the `ApiClient` interface (the single TDD seam) and the shared data models (`GameState`, `Ad`, `ShopItem`, `SolveResult`, `GameReport`, `Logger`) with NO logic — pure `interface`/`type` declarations only. Wave-2/3 plans (decodeAd, FakeApiClient, HttpApiClient) all depend on this file.

Purpose: Establish the project skeleton and the contracts so Phases 2-4 can be developed and tested offline against the interface, not against `fetch`.
Output: A compiling, lintable, test-runnable project with `src/types.ts` defining all shared models and the two interfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap ESM TypeScript project (manifest, tsconfig, biome, deps)</name>
  <files>package.json, tsconfig.json, biome.json, .gitignore</files>
  <read_first>
    - CLAUDE.md (Technology Stack section — exact pinned versions; What NOT to Use table; ESM mandate)
    - .planning/research/STACK.md (Installation block — the exact `npm install` lines; tsconfig snippet starting line 59; "Inject the API client" decision)
    - .planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md (Locked Carry-Forwards: stack is Node 24 LTS / TS 5.9 NOT 6.0 / tsx / native fetch / Vitest / Pino+pino-pretty / Biome; ESM "type":"module"; tsc --noEmit; zod 4.x confirmed as the one allowed runtime dep beyond Pino per D-01)
  </read_first>
  <action>
    Create `package.json` with `"type": "module"`, `"engines": { "node": ">=24" }`, and a `"scripts"` block exactly: `dev` runs `tsx watch src/index.ts`, `start` runs `tsx src/index.ts`, `test` runs `vitest run`, `test:watch` runs `vitest`, `typecheck` runs `tsc --noEmit`, `lint` runs `biome check --write .`. (index.ts does not exist yet — that is Phase 4; the scripts may reference it now.) Install runtime deps `zod@^4` (the one boundary-validation dep per D-01), `pino@^10`, `pino-pretty@^13` (pino-pretty may be a regular dep for a CLI; do not over-optimize). Install dev deps: `typescript@~5.9` (PIN to ~5.9 — TS 6.0 is forbidden per CLAUDE.md), `tsx@^4`, `vitest@^4`, `@biomejs/biome@^2`, `@tsconfig/node24`, `@types/node`. Use the npm CLI to install so the lockfile and node_modules are real (run `npm install` for the deps); do NOT hand-write version numbers into package.json that you have not installed. Create `tsconfig.json` that `extends` `@tsconfig/node24` and sets `compilerOptions.noEmit: true`, `compilerOptions.strict: true`, and includes `src` (and `**/*.test.ts`). Create `biome.json` with a minimal Biome 2.x config enabling the recommended linter + formatter (assist/organizeImports on). Create `.gitignore` covering `node_modules/`, `dist/`, and editor/OS noise. Do NOT add axios/got/ky, nock/msw, winston, ts-node, or any DI container — all forbidden per CLAUDE.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --passWithNoTests 2>&1 | grep -qE 'No test files found|Test Files|passed' && npx biome check .</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"type": "module"` and an `"engines"` entry requiring node >=24
    - package.json `dependencies` includes `zod` (4.x) and `pino`; `devDependencies` includes `typescript` (5.9.x, NOT 6.x), `tsx`, `vitest`, `@biomejs/biome`, `@tsconfig/node24`, `@types/node`
    - The installed `typescript` version satisfies `~5.9` (run `npx tsc --version` → reports 5.9.x, not 6.x)
    - tsconfig.json `extends` `@tsconfig/node24` and sets `noEmit: true`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run --passWithNoTests` not only exits 0 but its output contains a recognizable Vitest discovery signal — `npx vitest run --passWithNoTests 2>&1 | grep -qE 'No test files found|Test Files|passed'` succeeds — proving the runner actually performed test discovery (guards against a silently-misconfigured include glob / bad config that exits 0 as a false-green no-op)
    - `npx biome check .` exits 0 (formatting/lint clean)
    - No forbidden package (axios, got, ky, nock, msw, winston, ts-node, tsyringe, inversify) appears in package.json
  </acceptance_criteria>
  <done>The project compiles, lints clean, and the test runner provably runs discovery (with zero tests, confirmed by a recognizable Vitest discovery line — not merely a 0 exit). All pinned versions match CLAUDE.md, TypeScript is 5.9.x not 6.x.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared models and the ApiClient + Logger interfaces in types.ts (no logic)</name>
  <files>src/types.ts</files>
  <read_first>
    - .planning/research/FEATURES.md (Response field reference, lines 31-67 — exact field names for game-info, ad, solve result, shop item, buy result; the solve/buy field asymmetry note line 63)
    - .planning/research/ARCHITECTURE.md (Pattern 2 Injectable Client Interface, lines 147-168 — the ApiClient interface sketch; Component Responsibilities table)
    - .planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md (D-02 encrypted typed as optional number NOT a strict 1|2 union; specifics: probability typed as string free text, NOT a union; Locked Carry-Forwards: solve omits level, buy omits score — models must make the merge possible)
    - CLAUDE.md (constraint: no TS enum/namespace/decorators — use string-literal unions / const objects)
  </read_first>
  <action>
    Create `src/types.ts` containing ONLY type declarations (no runtime logic, no imports of fetch/zod/pino). Declare: `GameState` with `gameId: string`, `lives: number`, `gold: number`, `level: number`, `score: number`, `highScore: number`, `turn: number` (the threaded game-info object). Declare `Ad` with `adId: string`, `message: string`, `reward: number` (already coerced to number by the boundary — string coercion happens in api.ts, not here), `expiresIn: number`, `probability: string` (free text — NOT a union, unknown labels must be tolerable per the Specific Ideas note), and `encrypted` as an OPTIONAL number (`encrypted?: number` — per D-02, NOT a strict `1|2` union, so unknown schemes type-check; after decodeAd a handled ad has this cleared). Declare `ShopItem` with `id: string`, `name: string`, `cost: number`. Declare `SolveResult` with `success: boolean`, `lives: number`, `gold: number`, `score: number`, `highScore: number`, `turn: number`, `message: string` (NOTE: no `level` — solve omits it). Declare a buy-result-shaped type if helpful (e.g. the `buy` method returns merged `GameState`; the raw buy wire shape has `shoppingSuccess`, `gold`, `lives`, `level`, `turn` and NO `score` — model whatever `api.ts` needs to merge correctly). Declare `GameReport` with at least `score: number`, `turns: number`, and an end-reason field (e.g. `reason: string`). Declare the `ApiClient` interface with exactly these async methods returning typed models: `startGame(): Promise<GameState>`, `getMessages(gameId: string): Promise<Ad[]>`, `solve(gameId: string, adId: string): Promise<SolveResult>`, `getShop(gameId: string): Promise<ShopItem[]>`, `buy(gameId: string, itemId: string): Promise<GameState>`. Declare a `Logger` interface with leveled methods `debug`, `info`, `warn`, `error` (each accepting a message and optional structured args). Use string-literal unions / plain interfaces — NO `enum`, NO `namespace`, NO decorators.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^[[:space:]]*//' src/types.ts | grep -q 'export interface ApiClient' && grep -v '^[[:space:]]*//' src/types.ts | grep -q 'export interface Logger'</automated>
  </verify>
  <acceptance_criteria>
    - types.ts exports `interface ApiClient` with the five methods startGame/getMessages/solve/getShop/buy and the exact signatures above (source assertion: `grep -c 'startGame\|getMessages\|solve\|getShop\|buy' src/types.ts` ≥ 5 within the ApiClient interface)
    - types.ts exports `interface Logger` with debug/info/warn/error
    - `Ad.probability` is typed `string` (free text), NOT a string-literal union (so an unknown label compiles)
    - `Ad.encrypted` is an OPTIONAL number (`encrypted?: number`), NOT a `1 | 2` union (D-02)
    - `SolveResult` has NO `level` field; the game-state / buy path is shaped so `level` and `score` can be merged independently (Locked Carry-Forward: solve omits level, buy omits score)
    - types.ts imports nothing runtime (no `import` of fetch, zod, pino) — it is the leaf (source assertion: no `import` statement pulling a runtime dependency)
    - `npx tsc --noEmit` exits 0
    - No TS `enum`, `namespace`, or decorator appears in the file
  </acceptance_criteria>
  <done>The shared vocabulary and both interfaces compile cleanly; ApiClient is the declared seam; probability is free-text string and encrypted is an optional number; the file has zero runtime logic and zero runtime imports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verify the installed dependency set (supply-chain trust boundary)</name>
  <what-built>The greenfield project is bootstrapped (package.json ESM + scripts, tsconfig extending @tsconfig/node24, biome.json, .gitignore, real node_modules from `npm install`) and `src/types.ts` defines all shared models plus the `ApiClient` and `Logger` interfaces with no logic. `npx tsc --noEmit`, `npx vitest run --passWithNoTests`, and `npx biome check .` all pass. This checkpoint exists because installing packages from the registry crosses a supply-chain trust boundary (T-01-SC) — the human confirms the installed dependency set is the intended, legitimate one before later plans build on it.</what-built>
  <action>Pause execution and have the human verify the installed dependency set and toolchain before any downstream plan builds on it. Present the results of `npm ls --depth=0`, `npx tsc --version`, `npx tsc --noEmit`, and `npx vitest run --passWithNoTests`, plus the `src/types.ts` ApiClient/Ad contract, then wait for explicit approval. Do NOT auto-approve — this is a blocking supply-chain trust gate (T-01-SC).</action>
  <how-to-verify>
    1. Run `npm ls --depth=0` and confirm the direct dependencies are exactly: zod, pino, pino-pretty, typescript, tsx, vitest, @biomejs/biome, @tsconfig/node24, @types/node — and nothing unexpected (no axios/got/ky/nock/msw/winston/ts-node).
    2. Run `npx tsc --version` and confirm it reports a 5.9.x version (NOT 6.x).
    3. Run `npx tsc --noEmit` and confirm it exits 0.
    4. Run `npx vitest run --passWithNoTests` and confirm it exits 0.
    5. Open `src/types.ts` and confirm `ApiClient` lists startGame/getMessages/solve/getShop/buy, `Ad.probability` is `string`, and `Ad.encrypted` is `encrypted?: number`.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the installed dependency set matches the intended list with no unexpected packages
    - Human confirms TypeScript is 5.9.x
    - Human confirms tsc, vitest, and the types.ts contract are correct
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed, or describe any wrong/unexpected dependency or type-shape issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local project | Installing packages pulls third-party code that runs in dev/build (supply-chain surface) |
| (none inbound) | This plan creates only declarations + config; no runtime I/O, no untrusted input is processed yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (zod, pino, pino-pretty, tsx, vitest, biome, @tsconfig/node24, @types/node) | mitigate | Pin versions per CLAUDE.md (TS ~5.9, others to documented majors); blocking human checkpoint (Task 3) verifies the installed dependency set before downstream plans build on it; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — all are mainstream, high-download, documented in STACK.md |
| T-01-01 | Tampering | TypeScript version drift to 6.0 | mitigate | Pin `~5.9`; acceptance criterion asserts `tsc --version` is 5.9.x; TS 6.0 forbidden per CLAUDE.md |
| T-01-02 | Information Disclosure | secrets/node_modules committed to git | accept | `.gitignore` excludes node_modules; no secrets exist in this phase (no auth, no env secrets beyond an optional base-URL override added in Plan 04) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (project type-checks with strict on)
- `npx vitest run --passWithNoTests` exits 0 AND its output contains a recognizable Vitest discovery line (`grep -qE 'No test files found|Test Files|passed'` succeeds) — the runner provably ran test discovery, not a misconfigured no-op
- `npx biome check .` exits 0 (lint/format clean)
- `src/types.ts` exports `ApiClient` and `Logger`; `Ad.probability: string`; `Ad.encrypted?: number`
- No forbidden dependency present in package.json
</verification>

<success_criteria>
- A compiling, lintable, test-runnable ESM TypeScript project exists (Node 24 / TS 5.9 / tsx / Vitest / Biome)
- `src/types.ts` defines GameState, Ad, ShopItem, SolveResult, GameReport, ApiClient, Logger with no logic and no runtime imports
- The `ApiClient` interface (the TDD seam) is in place so every later phase depends on the interface, not on fetch (ROADMAP success criterion 1; supports API-01..API-04 by typing start/messages/solve/shop+buy)
- Human has confirmed the installed dependency set
</success_criteria>

<output>
Create `.planning/phases/01-foundation-types-api-client-test-seam/01-01-SUMMARY.md` when done.
</output>
