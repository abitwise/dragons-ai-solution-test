---
phase: 02-strategy-core-pure-decision-logic-tdd
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/strategy.ts
  - src/strategy.test.ts
autonomous: true
requirements: [STRAT-01, STRAT-02, TEST-01]

must_haves:
  truths:
    - "Every one of the 11 probability strings maps to its integer rank 0-10 (D-01)"
    - "The exact four-dot string 'Hmmm....' maps to rank 6 (D-01)"
    - "An unknown/new probability label ranks 0 (worst) and rankProbability never throws (D-01)"
    - "The eligibility filter drops expired ads (expiresIn <= 0), sub-floor ads (rank < 6), and still-encrypted ads (D-02/D-03)"
    - "The filter never mutates its input array and never throws"
  artifacts:
    - path: "src/strategy.ts"
      provides: "Rank lookup table, PROBABILITY_FLOOR_RANK constant, rankProbability, and the eligibility filter — the foundation strategy module"
      contains: "PROBABILITY_FLOOR_RANK"
      min_lines: 40
    - path: "src/strategy.test.ts"
      provides: "Table-driven rank tests (all 11 labels + unknown) and eligibility-filter tests, plain-object fixtures, no mocks"
      contains: "Hmmm...."
      min_lines: 60
  key_links:
    - from: "src/strategy.ts"
      to: "src/types.js"
      via: "type-only import of Ad"
      pattern: "import type.*\\{[^}]*Ad[^}]*\\}.*from \"\\./types\\.js\""
    - from: "src/strategy.test.ts"
      to: "src/strategy.js"
      via: "named import of rankProbability + the filter"
      pattern: "from \"\\./strategy\\.js\""
---

<objective>
Establish `src/strategy.ts` as the pure functional-core decision module and drive its first two responsibilities entirely test-first: (1) probability-string ranking via an exact-string lookup table (STRAT-01), and (2) ad eligibility filtering (STRAT-02). This is the foundation plan — it creates the file, its module header, the named-constant config block (including `PROBABILITY_FLOOR_RANK = 6`), and the rank table that every later plan in this phase consumes.

Purpose: The rank table and floor constant are the contract the EV selector (Plan 02) and the fallback both build on. Defining them first (interface-first ordering) prevents later plans from re-deriving the ranking. Building ranking + filtering test-first locks in the exact-string and never-throw guarantees that PITFALLS #6 warns are the most fragile part of the whole bot.

Output: `src/strategy.ts` with `rankProbability` and the eligibility filter; `src/strategy.test.ts` proving all 11 labels (incl. exact `"Hmmm...."`), unknown-label-worst-never-throw, and the three filter drop-rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-strategy-core-pure-decision-logic-tdd/02-CONTEXT.md
@.planning/phases/02-strategy-core-pure-decision-logic-tdd/02-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@src/types.ts
@src/decode.ts
@src/decode.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for rankProbability (STRAT-01)</name>
  <files>src/strategy.test.ts</files>
  <read_first>
    - src/decode.test.ts (THE style template: Vitest named imports `{ describe, expect, it }` alphabetized, `import type` for types via `./types.js`, the `baseAd(overrides): Ad` fixture-builder idiom, the `decoded.encrypted ?? 0` assertion, `expect(...).not.toThrow()` pattern at lines 157-159)
    - src/types.ts (the `Ad` shape: `probability: string` free-text, `encrypted?: number`)
    - .planning/research/FEATURES.md (lines 74-86: the verified 11 probability strings table with integer ranks 0-10; note line 80 — exact four-dot `"Hmmm...."`)
    - .planning/research/PITFALLS.md (Pitfall 6: string-equality fragility; the literal `"Hmmm...."` has exactly four dots; unknown label must not silently sort as NaN)
    - .planning/phases/02-strategy-core-pure-decision-logic-tdd/02-CONTEXT.md (D-01: exact-string lookup, integer ranks 0-10, unknown ranks worst=0, never throws)
    - .planning/phases/02-strategy-core-pure-decision-logic-tdd/02-PATTERNS.md (the describe/it nesting + naming pattern, table-driven assertion pattern, header doc-comment pattern)
  </read_first>
  <behavior>
    - Each of the 11 labels maps to its exact integer rank: "Sure thing"=10, "Piece of cake"=9, "Walk in the park"=8, "Quite likely"=7, "Hmmm...."=6, "Gamble"=5, "Risky"=4, "Rather detrimental"=3, "Playing with fire"=2, "Suicide mission"=1, "Impossible"=0.
    - The exact four-dot string "Hmmm...." maps to 6 (assert with the literal four-dot string, not a three-dot or trailing-space variant).
    - An unknown label (e.g. "some new label", "", a base64-looking "U3VyZSB0aGluZw==") maps to 0.
    - rankProbability never throws for any string input — assert with `expect(() => rankProbability("anything")).not.toThrow()`.
  </behavior>
  <action>
    Create `src/strategy.test.ts`. Open with a short header doc-comment (mirror `decode.test.ts` lines 5-9) listing what the suite proves and citing STRAT-01..02 + D-01..D-03. Import Vitest `{ describe, expect, it }` (alphabetized) and the unit-under-test via `./strategy.js` (import `rankProbability` and the eligibility filter you will name in Task 2). Import `Ad` via `import type ... from "./types.js"`.

    Write a `describe("rankProbability")` block. Cover all 11 labels — a table-driven `it.each` over `[label, expectedRank]` pairs OR one explicit `it` per label, matching the analog's explicit-case prose style. Assert scalar equality with `toBe`. Add a nested `describe("known labels (D-01)")` with an explicit `it("maps the exact four-dot 'Hmmm....' to rank 6")` using the literal string `"Hmmm...."`. Add `describe("unknown label (D-01 worst-and-never-throw)")` asserting an unseen label returns 0 and `expect(() => rankProbability("unknown")).not.toThrow()`.

    Do NOT implement anything in `src/strategy.ts` yet — these tests MUST fail at the import/undefined-export stage (RED). Run `npx vitest run src/strategy.test.ts` and confirm failure. Commit RED: `test(02-01): add failing tests for rankProbability`.

    Exact ranks come from FEATURES.md lines 74-86 — do NOT use the MEDIUM-confidence approximate percentages; integer ranks are the weighting (D-01).
  </action>
  <verify>
    <automated>npx vitest run src/strategy.test.ts 2>&1 | grep -qE "(fail|FAIL|Cannot find|is not a function|not exported)" && echo "RED confirmed (tests fail before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/strategy.test.ts` exists and imports `rankProbability` from `./strategy.js`
    - Test file asserts `rankProbability("Hmmm....")` equals 6 using the exact four-dot literal
    - Test file asserts all 11 labels map to ranks 10..0 in the documented order
    - Test file asserts an unknown label returns 0 and `expect(() => rankProbability("x")).not.toThrow()`
    - `npx vitest run src/strategy.test.ts` FAILS (RED) — the export does not yet exist
    - A RED commit `test(02-01): ...` exists in git log
  </acceptance_criteria>
  <done>The rank tests are written, fail for the right reason (missing export, not syntax), and are committed as the RED gate.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN+RED — implement rankProbability, then add failing filter tests + implement (STRAT-01, STRAT-02)</name>
  <files>src/strategy.ts, src/strategy.test.ts</files>
  <read_first>
    - src/decode.ts (THE implementation style template: JSDoc file header citing decision IDs and asserting purity at lines 1-23; `import type ... from "./types.js"` at line 23; named `const` config UPPER_SNAKE at lines 25-26; un-exported helpers then exported total functions; the `switch`/`default` and early-return never-throw pattern at lines 76-121; return-new-object-via-spread purity at lines 88-121)
    - src/types.ts (the `Ad` interface: `probability: string`, `encrypted?: number`, `expiresIn: number`)
    - .planning/research/FEATURES.md (lines 74-86: the rank table data)
    - .planning/phases/02-strategy-core-pure-decision-logic-tdd/02-CONTEXT.md (D-01 unknown->0; D-02 floor = rank >= 6, drop Gamble=5 and riskier; D-03 the filter drops in ONE place: expired `expiresIn <= 0`, sub-floor rank < 6, AND still-encrypted ads — those still carrying an `encrypted` flag from Phase 1 D-09)
    - .planning/phases/01-foundation-types-api-client-test-seam/01-CONTEXT.md (the encrypted pass-through contract: a handled ad has `encrypted` cleared to 0/undefined; an unhandled ad arrives STILL flagged — the filter drops the still-flagged ones)
    - src/decode.ts (lines 94-121: how `decodeAd` clears the flag to `encrypted: 0` on success — so "still encrypted" means `encrypted` is a truthy non-zero number)
    - .planning/phases/02-strategy-core-pure-decision-logic-tdd/02-PATTERNS.md (`noUncheckedIndexedAccess` -> `?? 0` idiom; named-const pattern; pure never-mutate filter returning a new array)
  </read_first>
  <behavior>
    - rankProbability: `RANK[label] ?? 0` — known label returns its rank, unknown returns 0, never throws (GREEN for Task 1's tests).
    - eligibility filter: returns a NEW array containing only ads where ALL hold: `expiresIn > 0`, `rankProbability(ad.probability) >= PROBABILITY_FLOOR_RANK` (6), and the ad is NOT still-encrypted (a truthy non-zero `encrypted` flag means the client could not decode it -> drop).
    - filter drops a `Gamble` (rank 5) ad, a `Risky` (rank 4) ad, an `expiresIn: 0` ad, and an `encrypted: 1` (or `: 2`) ad; keeps a `Sure thing` plaintext non-expired ad.
    - filter is pure: input array and its ad objects are never mutated.
  </behavior>
  <action>
    GREEN for Task 1: Create `src/strategy.ts`. Open with a JSDoc file header (mirror `decode.ts` lines 1-23) naming the module, stating it is the pure decision core that imports ONLY `types.js` (no `fetch`/`zod`/`pino`/`ApiClient`/network) and never throws, and citing STRAT-01..06 + D-01..D-12 (this file will grow across plans 02-04). Add the type-only import `import type { Ad } from "./types.js";` (widen to include `GameState, ShopItem, SolveResult, BuyResult` as later plans need them — for THIS plan only `Ad` is required).

    Add the named-constant block: `const PROBABILITY_FLOOR_RANK = 6;` with a comment "Hmmm.... and safer (D-02)". Define the rank table as a `const RANK: Record<string, number>` keyed by the EXACT 11 strings -> integer ranks (FEATURES.md lines 74-86), with `"Hmmm...."` (four dots) -> 6. Implement `export function rankProbability(probability: string): number { return RANK[probability] ?? 0; }` — under `noUncheckedIndexedAccess` the lookup is `number | undefined`, so `?? 0` is the entire unknown->worst rule (D-01), no `if`/throw. Run `npx vitest run src/strategy.test.ts` — Task 1's tests MUST now pass. Commit GREEN: `feat(02-01): implement rankProbability with exact-string rank table`.

    RED for the filter: In `src/strategy.test.ts` add a `baseAd(overrides): Ad` fixture-builder (copy the `decode.test.ts` lines 22-32 idiom: complete valid plaintext ad spread-merged with overrides; default `probability: "Sure thing"`, `expiresIn: 3`, no `encrypted`). Add a `describe` for the eligibility filter (name at your discretion, e.g. `filterEligibleAds` or `eligibleAds`): assert it keeps a `Sure thing`/non-expired/plaintext ad; drops a `Gamble` (rank 5) ad (D-02); drops a `Risky` (rank 4) ad; drops an `expiresIn: 0` ad (D-03); drops an ad with `encrypted: 1` (D-03/D-09 still-encrypted); and does not mutate the input array (snapshot the input, assert `toEqual` after). Run the suite — the filter export does not exist yet, so these new tests FAIL (RED). Commit RED: `test(02-01): add failing tests for ad eligibility filter`.

    GREEN for the filter: Implement and export the filter in `src/strategy.ts`. It returns `ads.filter(...)` (a new array) keeping ads where `ad.expiresIn > 0 && rankProbability(ad.probability) >= PROBABILITY_FLOOR_RANK && !ad.encrypted` (a truthy non-zero `encrypted` means still-undecoded; `0`/`undefined` is decoded/plaintext per decode.ts clearing to `0`). Pure — `Array.filter` returns a new array and never mutates. Run the suite — all tests pass. Commit GREEN: `feat(02-01): implement ad eligibility filter (expired/sub-floor/still-encrypted)`.

    Run `npx biome check --write src/strategy.ts src/strategy.test.ts` and `npx tsc --noEmit` before the final commit. Do NOT inline fenced code in this plan — match the analog files you read.
  </action>
  <verify>
    <automated>npx vitest run src/strategy.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/strategy.ts` exists, imports ONLY from `./types.js` via `import type` (assert: no `fetch`, `zod`, `pino`, `api.js`, or `fake-api-client.js` import appears — `grep -v '^ *\*' src/strategy.ts | grep -cE "(fetch|from \"zod\"|from \"pino\"|api\.js|fake-api-client)"` returns 0)
    - `src/strategy.ts` defines `PROBABILITY_FLOOR_RANK` and a rank table with `"Hmmm...."` mapped to 6
    - `rankProbability("Hmmm....") === 6` and `rankProbability("nonexistent label") === 0` (proven by passing tests)
    - The eligibility filter drops `Gamble`/`Risky`/`expiresIn:0`/`encrypted:1` ads and keeps a `Sure thing` plaintext non-expired ad (proven by passing tests)
    - A non-mutation test proves the filter does not mutate its input array
    - `npx vitest run src/strategy.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
    - GREEN commits `feat(02-01): ...` for both rankProbability and the filter exist in git log, each preceded by a `test(02-01): ...` RED commit
  </acceptance_criteria>
  <done>rankProbability and the eligibility filter are implemented, all Plan-01 tests pass, typecheck is clean, and the RED->GREEN commit sequence is present for both features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> strategy.ts | The runner (Phase 3) passes already-decoded, zod-validated plain objects. `strategy.ts` performs NO I/O — no network, no file reads, no deserialization, no secrets. The only "untrusted" input is malformed/adversarial plain-object data (unknown probability strings, missing/NaN fields). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service (robustness) | `rankProbability` on an unknown/adversarial label | mitigate | `RANK[label] ?? 0` returns worst rank and never throws (D-01) — a new/garbage label can never crash the decision loop. Covered by the never-throw test. |
| T-02-02 | Tampering (data robustness) | eligibility filter on a still-encrypted or malformed ad | mitigate | Filter drops still-encrypted ads (`encrypted` truthy) so an undecodable adId never reaches `/solve` (prevents the PITFALLS #2 400). Pure, no mutation, no throw. |

No high-severity threats apply: this is a pure functional-core module with no network, no filesystem, no deserialization, no secrets, and no untrusted-input parsing (the API client + zod validation are Phase 1; the runner is Phase 3). The honest concern is total-function robustness, which the "unknown ranks worst, never throws" contract already mitigates. No package installs in this plan, so the supply-chain checkpoint (T-{phase}-SC) does not apply.
</threat_model>

<verification>
- `npx vitest run src/strategy.test.ts` passes (all rank + filter tests green)
- `npx tsc --noEmit` passes
- `npx biome check src/strategy.ts src/strategy.test.ts` reports no issues
- `src/strategy.ts` imports only `./types.js` (no runtime imports)
</verification>

<success_criteria>
- All 11 probability labels map to their integer ranks 0-10; `"Hmmm...."` (exact four dots) -> 6
- An unknown label -> 0 and `rankProbability` never throws
- The eligibility filter drops expired, sub-floor (rank < 6), and still-encrypted ads in one place, returning a new array without mutating its input
- Tests are plain-object fixtures only — no mocks, no `FakeApiClient`, no network
- RED->GREEN TDD discipline visible in git history
</success_criteria>

<output>
Create `.planning/phases/02-strategy-core-pure-decision-logic-tdd/02-01-SUMMARY.md` when done, recording RED (tests written + why they failed), GREEN (rankProbability + filter implementation), any REFACTOR, and the commit list.
</output>
