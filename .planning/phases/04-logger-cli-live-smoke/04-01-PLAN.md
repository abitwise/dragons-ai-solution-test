---
phase: 04-logger-cli-live-smoke
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/logger.ts
  - src/logger.test.ts
autonomous: true
requirements: [LOG-01]
must_haves:
  truths:
    - "ConsoleLogger routes each level method (debug/info/warn/error) to the matching pino method"
    - "A message-first call with one object arg folds to pino's (mergeObj, message) idiom"
    - "A message-only call (zero args) passes just the message to pino"
    - "createConsoleLogger builds a real pino instance backed by a synchronous pino-pretty stream"
    - "logger.ts is the only module importing pino / pino-pretty"
  artifacts:
    - path: "src/logger.ts"
      provides: "ConsoleLogger class (implements Logger) + createConsoleLogger factory"
      exports: ["ConsoleLogger", "createConsoleLogger"]
      contains: "class ConsoleLogger implements Logger"
      min_lines: 40
    - path: "src/logger.test.ts"
      provides: "Offline spy coverage of level routing + D-02 fold"
      contains: "ConsoleLogger"
  key_links:
    - from: "src/logger.ts"
      to: "pino"
      via: "value import + pino(opts, stream) construction"
      pattern: "import pino from \"pino\""
    - from: "src/logger.ts"
      to: "pino-pretty"
      via: "value import + pinoPretty({ sync: true })"
      pattern: "import pinoPretty from \"pino-pretty\""
    - from: "src/logger.ts"
      to: "./types.js Logger interface"
      via: "type-only import + implements"
      pattern: "implements Logger"
---

<objective>
Create `src/logger.ts` — the concrete `ConsoleLogger` that implements the existing
`Logger` interface (`src/types.ts:131-136`) backed by Pino + pino-pretty (D-01), plus the
`createConsoleLogger(level)` production factory. This is the ONLY module in the codebase
that imports `pino`/`pino-pretty`; every other module depends on the `Logger` interface.

The class bridges the interface's message-first call shape (`method(message, ...args)`) to
pino's object-first idiom (`method(mergeObj, message)`) via a small pure `foldArgs` helper
(D-02). The constructor takes an INJECTED pino instance so `logger.test.ts` can spy on it
offline (mirrors `HttpApiClient`'s injectable `delay` in `src/api.ts:166-182`); the factory
wires the real synchronous pretty stream for production.

Purpose: satisfies the LOG-01 "human-readable, leveled output" mechanism that `runner.ts`
narration (Plan 02) and `index.ts` (Plan 03) both depend on.
Output: `src/logger.ts`, `src/logger.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-logger-cli-live-smoke/04-CONTEXT.md
@.planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md
@.planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md
@src/types.ts
@src/api.ts
@src/api.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: ConsoleLogger fold + level routing (RED → GREEN → REFACTOR)</name>
  <files>src/logger.ts, src/logger.test.ts</files>
  <read_first>
    - src/types.ts (lines 126-136 — the exact `Logger` interface signatures to implement)
    - src/api.ts (lines 28-30 import discipline; lines 52-77 error-class house style; lines 166-182 the injectable-dep + factory pattern to mirror)
    - src/api.test.ts (the `vi.spyOn` offline-seam + restoreAllMocks convention)
    - src/runner.test.ts (lines 49-56 — the silent `Logger` fixture; lines 1-17 file-header style)
    - .planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md (Pattern 1 + Pattern 2: verified `foldArgs` + emit shapes; Validation Architecture: the verified spy test)
    - .planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md (`src/logger.ts` + `src/logger.test.ts` sections — house-style analog and verified test shape)
  </read_first>
  <behavior>
    - Test (routing): `new ConsoleLogger(p).info("game started", { gameId: "g1", lives: 3 })` calls `p.info` with `({ gameId: "g1", lives: 3 }, "game started")`.
    - Test (zero-args fold): `.warn("no eligible ad")` calls `p.warn` with just `("no eligible ad")` (no merge object).
    - Test (each level routes): `.debug(...)`/`.info(...)`/`.warn(...)`/`.error(...)` each fire the same-named pino method and no other.
    - Test (multi/mixed fold, defensive): `.warn("skipped ad", { adId: "x" }, "extra", 42)` calls `p.warn` with `({ args: ["...the three args..."] }, "skipped ad")` — message stays the headline, extras wrapped under one `args` key.
    - Test (array arg is wrapped, not spread): a single array arg (e.g. `.debug("candidates", [adA, adB])`) folds to `({ args: [[adA, adB]] }, "candidates")` — an array is NOT treated as a merge object (the `!Array.isArray` guard).
  </behavior>
  <action>
    RED: write `src/logger.test.ts` first (colocated, NOT a `tests/` dir — tsconfig includes `**/*.test.ts`). Open with a JSDoc file header (mirror `src/runner.test.ts:7-17`). Build a `spyPino()` helper that constructs an OUTPUT-DISABLED pino instance — `pino({ level: "debug", enabled: false })` so it emits nothing (no stream, no pretty, no network) — and `vi.spyOn`s `info`/`warn`/`debug`/`error` on it. Add the five behavior tests above asserting CALL SHAPE only (`toHaveBeenCalledWith(...)`), NEVER the rendered pretty string (PITFALLS anti-pattern). Run `npx vitest run src/logger.test.ts` and confirm it FAILS (no `ConsoleLogger` yet).

    GREEN: write `src/logger.ts`. Open with the house JSDoc header stating "the ONLY module that imports `pino`/`pino-pretty`" and listing boundaries (mirror `src/runner.ts:1-24`). Value-import `pino` and `pinoPretty` (default imports — both are callable CJS factories, correct under `verbatimModuleSyntax`); `import type { Logger } from "./types.js"`. Declare `type LevelMethod = "debug" | "info" | "warn" | "error"` (string-literal union — NEVER a TS enum, per CLAUDE.md). Implement the pure `foldArgs(args: unknown[]): Record<string, unknown> | undefined` per RESEARCH Pattern 2: zero args → `undefined`; exactly one non-null non-array object → that object as the merge object; otherwise → `{ args }` (wrap so the message stays the headline). Implement `class ConsoleLogger implements Logger` with `constructor(private readonly p: pino.Logger) {}` (injected instance, mirrors `api.ts` injectable `delay`). A private `emit(level, message, args)` calls `foldArgs`; when `undefined` calls `this.p[level](message)`, else `this.p[level](obj, message)`. The four interface methods delegate to `emit`. Run `npx vitest run src/logger.test.ts` — confirm PASS.

    REFACTOR (if needed): tidy comments/naming so the message-first→object-first bridge reads clearly; re-run the test and keep it green. Do NOT add try/catch — logging is fire-and-forget and `logger.ts` does no throwing I/O (PATTERNS "Error handling: none needed").
  </action>
  <verify>
    <automated>npx vitest run src/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/logger.test.ts` exits 0
    - `src/logger.ts` contains `class ConsoleLogger implements Logger`
    - `grep -nE "type LevelMethod = " src/logger.ts` finds a string-literal union (no `enum` keyword anywhere in the file: `grep -c "enum" src/logger.ts` is `0`)
    - Test asserts `info` called with `({ gameId: "g1", lives: 3 }, "game started")` (object-first fold) AND `warn` called with `("no eligible ad")` (message-only)
  </acceptance_criteria>
  <done>
    ConsoleLogger implements all four Logger methods, routes each to the same-named pino
    method, folds message-first args into pino's object-first idiom, and is proven by an
    offline spy test (zero network) covering routing + all fold branches.
  </done>
</task>

<task type="execute">
  <name>Task 2: createConsoleLogger production factory (synchronous pretty stream)</name>
  <files>src/logger.ts</files>
  <read_first>
    - src/logger.ts (the class written in Task 1 — the factory returns `new ConsoleLogger(...)`)
    - src/api.ts (lines 166-182 — the `realDelay` + factory-with-defaults pattern to mirror)
    - .planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md (Pattern 1 verified pino sync-stream shape; Pitfall 1 — why NOT the worker-thread `transport:` form; Pitfall 3 — verbatimModuleSyntax import notes)
    - .planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md (`src/logger.ts` "Production factory" block)
  </read_first>
  <action>
    Add `export function createConsoleLogger(level: string): ConsoleLogger` to `src/logger.ts`.
    It builds the pretty stream with `pinoPretty({ colorize: true, translateTime: "SYS:HH:MM:ss", ignore: "pid,hostname", sync: true })` and returns `new ConsoleLogger(pino({ level }, stream))`. Use the SYNCHRONOUS stream form `pino(opts, stream)` (RESEARCH Pattern 1) — do NOT use the worker-thread `transport: { target: "pino-pretty" }` form (Pitfall 1: async flush can drop the final lines on exit of a short-lived CLI). The `sync: true` option plus `index.ts`'s `process.exitCode`+return discipline (Plan 03) guarantees deterministic flush. `colorize`/`translateTime`/`ignore` are Claude's discretion (D-03) — these are the verified-good defaults. Hoist the pretty-options into a named `const` with a one-line comment if it reads cleaner (house style). Keep this factory the ONLY place the real stream is constructed.

    This factory is exercised end-to-end by the manual live smoke (Plan 04); it is intentionally NOT unit-tested against real output (asserting on pretty strings is an anti-pattern). The offline tests cover the class via the injected-pino seam (Task 1).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (`tsc --noEmit`) exits 0
    - `grep -c "transport:" src/logger.ts` is `0` (no worker-thread transport form)
    - `grep -E "sync:\s*true" src/logger.ts` matches (synchronous stream)
    - `grep -E "export function createConsoleLogger" src/logger.ts` matches
    - `npm run lint` (`biome check --write .`) exits 0
  </acceptance_criteria>
  <done>
    createConsoleLogger(level) returns a production ConsoleLogger backed by a synchronous
    pino-pretty stream; typecheck and lint are green; the worker-thread transport form is
    absent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response fields → log output | Untrusted ad messages / shop names / encrypted text reach the logger as structured fields when callers pass them as args |
| (none inbound) | This is a local CLI: no inbound network, no auth, no DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Log injection / forged log lines from untrusted API strings (ad message, encrypted text) rendered into output | mitigate | `foldArgs` keeps caller-supplied values as STRUCTURED pino FIELDS (the merge object), never concatenated into the message string — pino serializes them as data, so embedded ANSI/newline sequences cannot forge new log lines. Per D-02 the message is always the caller's literal headline; untrusted values ride in the object. |
| T-04-02 | Information Disclosure | Sensitive data in logs (raw API objects dumped) | accept | This game API carries NO credentials/PII (stated assumption). Raw objects only ride as DEBUG-level fields here; the DEBUG-only discipline for raw arrays/objects is enforced in `runner.ts` narration (Plan 02), not in `logger.ts`. `logger.ts` itself logs nothing on its own. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed this phase — `pino`/`pino-pretty` were vetted and installed in prior phases (RESEARCH: "Package Legitimacy Audit is N/A"). No install task exists in this plan. |
</threat_model>

<verification>
- `npx vitest run src/logger.test.ts` exits 0 (offline, no network)
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `logger.ts` imports pino/pino-pretty and `Logger`; no `console`/`fetch`/`enum`
</verification>

<success_criteria>
- ConsoleLogger implements the Logger interface and routes/folds correctly (proven offline)
- createConsoleLogger wires the real synchronous pretty stream (production factory)
- logger.ts is the sole pino/pino-pretty importer; no enum; suite stays offline
</success_criteria>

<output>
Create `.planning/phases/04-logger-cli-live-smoke/04-01-SUMMARY.md` when done
</output>
