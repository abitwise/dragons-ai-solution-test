---
phase: 04-logger-cli-live-smoke
plan: 03
type: tdd
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/index.ts
  - src/index.test.ts
autonomous: true
requirements: [LOG-02]
must_haves:
  truths:
    - "resolveLogLevel applies precedence: explicit flag > LOG_LEVEL env > default 'info'"
    - "--verbose / -v resolves to 'debug'; an unknown level value is rejected (falls through to env/default)"
    - "exitCodeForReason maps GAME_OVER→0 and TURN_CAP/NO_PROGRESS→1"
    - "index.ts constructs the real HttpApiClient + ConsoleLogger and injects them into playGame (the ONLY place)"
    - "On normal return the final-score block prints to stdout (visible at any log level) and exit code is 0/1"
    - "On a thrown Transport/BoundaryError the catch prints a failure line and sets exit code 2"
    - "main sets process.exitCode and returns (no process.exit()) so the sync stream + banner drain fully"
  artifacts:
    - path: "src/index.ts"
      provides: "CLI composition root: dep construction, verbosity resolution, banner, error catch, exit-code mapping"
      exports: ["resolveLogLevel", "exitCodeForReason"]
      contains: "new HttpApiClient"
      min_lines: 50
    - path: "src/index.test.ts"
      provides: "Offline unit coverage of resolveLogLevel precedence + exitCodeForReason mapping"
      contains: "exitCodeForReason"
  key_links:
    - from: "src/index.ts"
      to: "src/logger.ts createConsoleLogger"
      via: "value import + construction at resolved level"
      pattern: "createConsoleLogger"
    - from: "src/index.ts"
      to: "src/runner.ts playGame + END"
      via: "await playGame(api, logger); import { END } for reason→code mapping"
      pattern: "playGame\\(api, logger\\)"
    - from: "src/index.ts"
      to: "src/api.ts HttpApiClient + error classes"
      via: "construct HttpApiClient; catch Transport/BoundaryError → exit 2"
      pattern: "new HttpApiClient"
---

<objective>
Create `src/index.ts` — the CLI composition root and the ONLY place the real `HttpApiClient`
and `ConsoleLogger` are constructed and injected into `playGame` (success criterion #3,
LOG-02). It resolves the log level from flag+env (D-10), runs the game inside the single
authoritative try/catch (D-09), prints the always-visible final-score banner to stdout
(D-07), and maps the outcome to a 3-way exit code (D-08): `0` natural game-over, `1` guard
stop, `2` thrown error. Two pure helpers — `resolveLogLevel(argv, env)` and
`exitCodeForReason(reason)` — are exported and unit-tested offline in `src/index.test.ts`,
keeping the six-source-file ceiling (no 7th file — Open Question Q4 → export from index.ts).

It depends on Plan 01's `createConsoleLogger` and Plan 02's exported `END`.

Open Question Q2 resolution: when BOTH `--log-level <lvl>` and `--verbose` are passed,
`--log-level` WINS (it is the more explicit flag). Document this with a one-line comment.
Open Question Q3 resolution: SKIP the shebang / `bin` entry for v1 (run via `npm start`).

Purpose: completes LOG-02 — a clear final-score summary on game end plus a status code that
reflects the run outcome — and is the entrypoint the manual live smoke (Plan 04) executes.
Output: `src/index.ts`, `src/index.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-logger-cli-live-smoke/04-CONTEXT.md
@.planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md
@.planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md
@src/types.ts
@src/api.ts
@src/runner.ts
@src/runner.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: resolveLogLevel + exitCodeForReason pure helpers (RED → GREEN → REFACTOR)</name>
  <files>src/index.ts, src/index.test.ts</files>
  <read_first>
    - src/runner.ts (the EXPORTED `END` object from Plan 02 — its three exact reason strings; the playGame signature)
    - src/runner.test.ts (lines 189-196 — the `REASON` const that mirrors the END strings to catch drift; the describe/it permutation style)
    - src/types.ts (GameReport 101-105 — `reason: string`, what exitCodeForReason consumes)
    - .planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md (Pattern 3 verified `parseArgs` config + precedence; Pattern 5 verified exit-code mapping; Pitfall 2 — END is private→now exported; Validation Architecture: the verified offline tests; Open Questions Q1/Q2/Q4)
    - .planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md (`src/index.ts` resolveLogLevel + exitCodeForReason blocks; `src/index.test.ts` block)
  </read_first>
  <behavior>
    - resolveLogLevel(["--verbose"], {}) === "debug"
    - resolveLogLevel(["-v"], {}) === "debug"
    - resolveLogLevel([], { LOG_LEVEL: "warn" }) === "warn"
    - resolveLogLevel([], {}) === "info"
    - resolveLogLevel(["--log-level", "error"], { LOG_LEVEL: "warn" }) === "error"  (flag > env)
    - resolveLogLevel(["--log-level", "warn", "--verbose"], {}) === "warn"  (Q2: --log-level wins over --verbose)
    - resolveLogLevel([], { LOG_LEVEL: "WARN" }) === "warn"  (env is lowercased before validation)
    - resolveLogLevel([], { LOG_LEVEL: "bogus" }) === "info"  (unknown env level rejected → default)
    - resolveLogLevel(["--log-level", "nonsense"], { LOG_LEVEL: "warn" }) === "warn"  (unknown flag level rejected → falls through to env)
    - exitCodeForReason(END.GAME_OVER) === 0
    - exitCodeForReason(END.TURN_CAP) === 1
    - exitCodeForReason(END.NO_PROGRESS) === 1
  </behavior>
  <action>
    RED: write `src/index.test.ts` first (colocated). Import `{ resolveLogLevel, exitCodeForReason }`
    from `./index.js`. Mirror `runner.test.ts:189-196`: declare a local `REASON` const with the
    three END strings VERBATIM (drift-catcher — if a runner string changes, this test breaks).
    Add the behavior cases above as `describe`/`it` blocks. `resolveLogLevel` takes `(argv, env)`
    as explicit params (NOT reading `process.argv`/`process.env` internally) so the test is pure
    and offline. Run `npx vitest run src/index.test.ts` and confirm it FAILS (no index.ts yet).

    GREEN: write `src/index.ts` with the house JSDoc header ("the ONLY composition root — the
    only place `HttpApiClient` + `ConsoleLogger` are constructed"). Implement the two exported
    pure functions:
      - `PINO_LEVELS` as a `const` Set of the valid pino levels ("trace","debug","info","warn",
        "error","fatal","silent") — house const-object/set style, NOT an enum.
      - `export function resolveLogLevel(argv: string[], env: NodeJS.ProcessEnv): string` using
        `parseArgs` (from `node:util`) with options `{ verbose: { type: "boolean", short: "v" },
        "log-level": { type: "string" }, help: { type: "boolean", short: "h" } }`,
        `allowPositionals: false` (D-11), `strict: true`. Precedence (Q2 — `--log-level` wins):
        if `values["log-level"]` is set AND in `PINO_LEVELS` → return it; else if `values.verbose`
        → return "debug"; else if `env.LOG_LEVEL?.toLowerCase()` is in `PINO_LEVELS` → return it;
        else return "info". An unknown/bogus level is REJECTED (not in the set) so it falls
        through — a bogus value can never crash the logger or silently disable output (security
        T-04-03). Add a one-line comment documenting the Q2 precedence (--log-level > --verbose).
      - `export function exitCodeForReason(reason: string): 0 | 1` — import the EXPORTED `END`
        from `./runner.js` and return `reason === END.GAME_OVER ? 0 : 1` (TURN_CAP/NO_PROGRESS→1).
        Add the self-documenting comment: 0 = natural game-over, 1 = guard stop (2 is the catch
        path, Task 2). Using the imported `END` keeps the mapping DRY (Q1 option b).
    Run `npx vitest run src/index.test.ts` — confirm PASS.

    REFACTOR (if needed): tidy comments; keep both helpers pure and offline. Re-run green.

    Note: `parseArgs` in strict mode THROWS on an unknown FLAG (not value). The `main` wiring
    (Task 2) calls `resolveLogLevel` inside the try/catch so a bad flag surfaces as a clean
    exit-2 failure rather than an uncaught crash — keep `resolveLogLevel` itself throw-on-unknown-flag
    (do not swallow), the catch handles it.
  </action>
  <verify>
    <automated>npx vitest run src/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/index.test.ts` exits 0
    - `resolveLogLevel(["-v"], {})` returns `"debug"`; `resolveLogLevel([], { LOG_LEVEL: "warn" })` returns `"warn"`; `resolveLogLevel([], {})` returns `"info"`
    - `resolveLogLevel(["--log-level", "warn", "--verbose"], {})` returns `"warn"` (Q2: flag-over-verbose precedence proven)
    - `resolveLogLevel([], { LOG_LEVEL: "bogus" })` returns `"info"` (unknown level rejected)
    - `exitCodeForReason` returns `0` for GAME_OVER and `1` for TURN_CAP and NO_PROGRESS
    - `src/index.test.ts` re-declares the three END strings verbatim as a drift-catcher
    - `grep -c "enum" src/index.ts` is `0`
  </acceptance_criteria>
  <done>
    resolveLogLevel and exitCodeForReason are exported, pure, and proven offline across the full
    precedence + mapping matrix (including --log-level>--verbose and bogus-level rejection);
    exitCodeForReason consumes the imported END (DRY).
  </done>
</task>

<task type="execute">
  <name>Task 2: Composition root — construct deps, banner, single catch, exit-code wiring</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (the helpers from Task 1 — main composes them)
    - src/api.ts (lines 28-77 — HttpApiClient construction, TransportError/BoundaryError classes; line 177 the MUGLOAR_BASE_URL read that index.ts must NOT duplicate)
    - src/runner.ts (the playGame signature + exported END; the "index.ts owns the catch" boundary note 10-17)
    - src/logger.ts (createConsoleLogger factory from Plan 01 — index.ts calls it at the resolved level)
    - src/types.ts (GameReport 101-105 — what the banner prints and exitCodeForReason maps)
    - .planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md (Pattern 4 banner-to-stdout; Pattern 5 main()/catch + process.exitCode discipline; Anti-Patterns: NOT process.exit(), NOT worker transport)
    - .planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md (`src/index.ts` orchestration block; "process.env reads at the edge only")
  </read_first>
  <action>
    Complete `src/index.ts`. Value-import `parseArgs` (node:util), `HttpApiClient` (and
    optionally `BoundaryError`/`TransportError` for class-specific wording) from `./api.js`,
    `createConsoleLogger` from `./logger.js`, `playGame` + `END` from `./runner.js`; type-only
    import `GameReport` from `./types.js`. All relative imports carry `.js` (NodeNext).

    Add `printBanner(report: GameReport): void` — print a bordered, visually distinct
    `FINAL SCORE` block (score / turns / end reason) to stdout via `process.stdout.write`
    (D-07), so it is ALWAYS visible regardless of log level (it bypasses pino). Exact border
    art/wording is Claude's discretion (RESEARCH Pattern 4 is a verified shape).

    Add `async function main(): Promise<void>`:
      - `const level = resolveLogLevel(process.argv.slice(2), process.env)` then
        `const logger = createConsoleLogger(level)` then `const api = new HttpApiClient()`.
        This is the ONLY place the real pair is constructed (success criterion #3). Do NOT read
        or duplicate `MUGLOAR_BASE_URL` — `api.ts` owns it (PATTERNS "env reads at the edge only").
      - try: `const report = await playGame(api, logger)`; `printBanner(report)`;
        `process.exitCode = exitCodeForReason(report.reason)` (0 or 1, D-08).
      - catch(err): `logger.error("game crashed", { error: err instanceof Error ? err.message : String(err) })`;
        print a clear failure line to stdout (`process.stdout.write`); `process.exitCode = 2`
        (D-08 — ANY thrown Transport/BoundaryError → 2; `instanceof` refinement for nicer wording
        is optional). The `resolveLogLevel` call may also be moved inside the try if you want a
        bad flag to exit 2 cleanly — Claude's discretion, but document it.
      - Do NOT call `process.exit()` anywhere — set `process.exitCode` and RETURN so Node drains
        the synchronous pretty stream + the stdout banner fully (RESEARCH Pattern 5 / Pitfall 1
        / Anti-Patterns). End the file with `void main();`.
      - SKIP the `#!/usr/bin/env node` shebang / `bin` entry (Q3 — run via `npm start`).

    `playGame` still never catches (locked); `index.ts` is the single authoritative try/catch
    (D-09). Run `npm run typecheck` and `npm run lint` as hard gates (RESEARCH Pitfall 3 /
    Assumption A1 — confirm the pino/pino-pretty/node:util value imports typecheck under
    verbatimModuleSyntax).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (`tsc --noEmit`) exits 0
    - `npm run lint` (`biome check --write .`) exits 0
    - `npm test` (full `vitest run` suite) exits 0 — and makes ZERO live network calls (success criterion #4)
    - `grep -c "new HttpApiClient" src/index.ts` is `1` AND `grep -c "createConsoleLogger" src/index.ts` is `1` (the only construction site)
    - `grep -c "process.exit(" src/index.ts` is `0` (uses `process.exitCode`, not `process.exit()`)
    - `grep -cE "process\\.stdout\\.write" src/index.ts` is at least `1` (banner bypasses pino)
    - `grep -c "await playGame(api, logger)" src/index.ts` is `1`
    - No source file other than index.ts constructs the real pair: `grep -rl "new HttpApiClient\\|createConsoleLogger" src --include=*.ts | grep -v ".test.ts" | grep -v "logger.ts"` lists only `src/index.ts`
  </acceptance_criteria>
  <done>
    index.ts constructs the real HttpApiClient + ConsoleLogger (the only place), runs playGame
    in a single try/catch, prints the always-visible FINAL SCORE banner to stdout, and sets
    process.exitCode 0/1/2 without calling process.exit(); typecheck, lint, and the full offline
    suite are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args + env → log-level config | `--log-level`/`--verbose`/`-v` flags and `LOG_LEVEL` env are untrusted user input that selects the pino level |
| Live API response → GameReport → banner/exit | `playGame` consumes live API data; index.ts prints the derived report and maps its reason |
| (only outbound network) | index.ts triggers the real HTTP via HttpApiClient; nothing inbound, no auth, no DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | Untrusted `LOG_LEVEL` env / `--log-level` flag value | mitigate | `resolveLogLevel` validates the value against the closed `PINO_LEVELS` set; an unknown/bogus value is REJECTED and falls through to env/default `info` (never passed raw into pino), so a bogus value cannot crash the logger or silently disable all output. Proven by the `bogus`/`nonsense` test cases. |
| T-04-04 | Tampering | Unknown CLI flag injected | accept | `parseArgs({ strict: true })` THROWS on an unknown flag; the throw is caught by main's try/catch → clean exit 2 with a failure line (no uncaught crash, no partial run). Acceptable: a local single-user CLI; the strict parser is the mitigation, the catch is the backstop. |
| T-04-01 | Tampering | Log injection from untrusted API strings in the failure/error line | mitigate | The catch logs the error via `logger.error("game crashed", { error })` — the untrusted message rides as a STRUCTURED field, not concatenated into the headline. The stdout failure line prints the error message but it originates from our own typed Error classes (api.ts), not raw attacker-controlled response bytes. |
| T-04-05 | Information Disclosure | Final-score banner / failure line leaking sensitive data | accept | The banner prints only `score`/`turns`/`reason` (no secrets); the failure line prints an Error message from our own taxonomy. This game API carries no credentials/PII (stated assumption). No sensitive data is in scope to leak. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed this phase (RESEARCH: Package Legitimacy Audit N/A — pino/pino-pretty vetted in prior phases). `node:util` is stdlib. No install task in this plan. |
</threat_model>

<verification>
- `npx vitest run src/index.test.ts` exits 0 (offline pure-function coverage)
- `npm test` (full suite) exits 0 with zero live network calls
- `npm run typecheck` exits 0; `npm run lint` exits 0
- index.ts is the only construction site for HttpApiClient + ConsoleLogger
- No `process.exit()`; banner via `process.stdout.write`; exit codes 0/1/2 wired per D-08
</verification>

<success_criteria>
- index.ts resolves the log level (flag > env > info, --log-level > --verbose), constructs and
  injects the real pair, runs playGame in the single authoritative try/catch
- Final-score banner prints to stdout (visible at any level); exit code reflects outcome (0/1/2)
- Pure helpers proven offline; full suite green and network-free; typecheck + lint clean
</success_criteria>

<output>
Create `.planning/phases/04-logger-cli-live-smoke/04-03-SUMMARY.md` when done
</output>
