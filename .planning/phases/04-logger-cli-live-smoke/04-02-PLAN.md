---
phase: 04-logger-cli-live-smoke
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runner.ts
  - src/runner.test.ts
autonomous: true
requirements: [LOG-01]
must_haves:
  truths:
    - "Each turn's chosen ad + solve outcome (with lives/gold/score) is narrated at INFO"
    - "Each shop buy is narrated at INFO; a shoppingSuccess:false buy is narrated at WARN"
    - "A no-eligible-ad / empty-board turn is narrated at WARN (nothing-to-do)"
    - "Verbose play-by-play (candidate ads + EV detail, shop catalog, fetch boundaries) stays at DEBUG"
    - "The END reason vocabulary is exported from runner.ts so index.ts can map reasons DRY"
    - "playGame signature, loop mechanics, the two guards, and the END strings are unchanged"
  artifacts:
    - path: "src/runner.ts"
      provides: "Enriched logger.* narration (INFO/WARN/DEBUG taxonomy) + exported END"
      contains: "export const END"
    - path: "src/runner.test.ts"
      provides: "Behavioral assertions on emitted log-call levels via a recording Logger spy"
      contains: "playGame"
  key_links:
    - from: "src/runner.ts"
      to: "Logger interface (logger.info/warn/debug/error)"
      via: "interface-only calls, message-first"
      pattern: "logger\\.(info|warn|debug|error)\\("
    - from: "src/index.ts (Plan 03)"
      to: "src/runner.ts END export"
      via: "import { END } for reason→exit-code mapping"
      pattern: "export const END"
---

<objective>
Enrich `runner.ts`'s `logger.*` narration so each turn's decision and outcome is logged at
the right level (LOG-01, D-04/D-05/D-06), and export the `END` reason vocabulary so `index.ts`
(Plan 03) can map reasons to exit codes from a single source of truth (Open Question Q1 →
option (b): export, DRY).

This plan ONLY adds `logger.*` calls inside the existing, LOCKED loop and makes `END` a
named export. It does NOT change the `playGame(api, logger)` signature, the loop mechanics,
the `drainShop` flow, the two termination guards, or the three `END` strings — those are
locked by Phase 3 (CONTEXT scope_fence). The runner keeps calling ONLY the `Logger`
interface; it never imports `console`/`pino`/`pino-pretty` (boundary stated in its header).

Purpose: makes the brief's "human-readable, leveled logging" real at the turn level — a
default `info` run is one scannable line per decision/outcome/skip; `LOG_LEVEL=debug`
reveals the full reasoning.
Output: modified `src/runner.ts`, modified `src/runner.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-logger-cli-live-smoke/04-CONTEXT.md
@.planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md
@.planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md
@src/runner.ts
@src/runner.test.ts
@src/strategy.ts
@src/types.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Export END + enrich runner narration (INFO/WARN/DEBUG taxonomy)</name>
  <files>src/runner.ts</files>
  <read_first>
    - src/runner.ts (the WHOLE file — the locked loop, the four existing logger.* call sites at lines 123/143/152/158, the `drainShop` flow 75-92, the private `const END` at 42-46)
    - src/strategy.ts (lines 84, 107, 181, 223 — the `chooseAd`/`filterEligibleAds`/`rankProbability`/`chooseShopPurchase` signatures whose decisions you narrate; do NOT modify this file)
    - src/types.ts (Logger interface 131-136; GameReport 101-105; Ad/ShopItem/SolveResult/BuyResult shapes for the fields you log)
    - .planning/phases/04-logger-cli-live-smoke/04-CONTEXT.md (D-04/D-05/D-06 — the exact level taxonomy; D-04 in-scope statement)
    - .planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md (`src/runner.ts (MODIFY ...)` section — which existing line gets which level)
    - .planning/phases/04-logger-cli-live-smoke/04-RESEARCH.md (Pitfall 4 — keep INFO scannable, candidate/ranking at DEBUG only)
  </read_first>
  <action>
    Q1 (option b — export END): change `const END = {...}` at `src/runner.ts:42` to
    `export const END = {...}` (additive — the `as const` object and its three exact strings
    stay byte-identical; this does NOT touch loop mechanics, the signature, the guards, or the
    taxonomy, so it respects the "only ADD narration" spirit per CONTEXT scope_fence + RESEARCH
    Open Q1). Leave `StopReason` and everything else in the file structurally unchanged.

    Enrich the `logger.*` calls INSIDE the existing loop (NO control-flow changes), applying the
    D-05/D-06 taxonomy. Keep the message-FIRST, single-structured-object-SECOND call shape
    (e.g. `logger.info("chose ad", { adId, reward, probability })`) — `logger.ts`'s `foldArgs`
    bridges it to pino; do NOT reorder to pino's idiom (D-02 subtlety).
      - INFO (decisions/outcomes): KEEP the start line (`runner.ts:123`). For the solve branch,
        ADD an INFO line for the CHOSEN ad before solving (adId + reward + probability) and an
        INFO line for the solve OUTCOME after merging (success boolean + lives/gold/score). The
        existing `logger.debug("solved ad", ...)` at line 143 stays/moves to DEBUG (raw detail);
        the human-readable decision/outcome is the new INFO line(s) (PROMOTE the decision to INFO
        per PATTERNS, keep verbose detail at DEBUG). KEEP the guard-stop INFO (line 152) and
        game-over INFO (line 158). In `drainShop`, ADD an INFO line per SUCCESSFUL buy
        (item id/name + gold after).
      - WARN (skips / nothing-to-do): ADD a WARN line on the `ad === null` branch (no eligible
        ad / empty board — the D-14 no-progress path: `runner.ts:139`). ADD a WARN line when a
        buy returns `shoppingSuccess:false` (`drainShop`, the `!result.shoppingSuccess` point at
        `runner.ts:85`).
      - DEBUG (verbose play-by-play): ADD DEBUG lines for the candidate ads considered and their
        EV ranking each turn, the shop catalog seen, and fetch boundaries (getMessages/getShop).
        Raw arrays/objects ride ONLY at DEBUG (PITFALLS rule). Default `info` stays scannable —
        do NOT promote candidate/ranking/catalog lines to INFO (RESEARCH Pitfall 4).
      - ERROR: optional — the runner MAY `logger.error` before a thrown Transport/BoundaryError
        unwinds (Claude's discretion; `index.ts` is the authoritative catch regardless, D-09). If
        added, keep it a single line in context; do NOT add a try/catch (the runner re-throws
        verbatim — locked).

    Compute candidate/EV DEBUG detail WITHOUT importing new strategy internals beyond what is
    already imported (`chooseAd`/`chooseShopPurchase` from `strategy.js`). If a DEBUG line needs
    a ranked view of `ads`, derive it from the `ads` array already fetched — do NOT add a new
    `strategy.ts` export or change `strategy.ts` (LOCKED). Do NOT interpolate raw untrusted API
    strings (ad message, encrypted text) INTO the message text — pass them as structured object
    fields so pino renders them as data (log-injection mitigation T-04-01).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export const END" src/runner.ts` matches (END is now exported)
    - The three END strings are byte-identical to before: `grep -c "game over: lives reached 0" src/runner.ts` is `1`, `grep -c "stopped: max-turn cap reached" src/runner.ts` is `1`, `grep -c "stopped: no-progress guard tripped" src/runner.ts` is `1`
    - `playGame` signature unchanged: `grep -c "export async function playGame(api: ApiClient, logger: Logger): Promise<GameReport>" src/runner.ts` is `1`
    - Runner uses only the Logger interface: `grep -cE "\\b(console|pino|HttpApiClient|FakeApiClient)\\b" src/runner.ts` (excluding comments) is `0`
    - `grep -cE "logger\\.warn\\(" src/runner.ts` is at least `2` (no-eligible-ad + shoppingSuccess:false)
    - `npm run typecheck` exits 0 and `npx vitest run src/runner.test.ts` exits 0 (existing Phase-3 GameReport assertions still pass — narration is non-load-bearing for them)
  </acceptance_criteria>
  <done>
    END is exported; the loop narrates chosen ad + solve outcome + each buy at INFO, skips at
    WARN, and candidate/ranking/catalog/fetch detail at DEBUG — with no change to signature,
    mechanics, guards, or the three END strings. Phase-3 tests stay green.
  </done>
</task>

<task type="execute">
  <name>Task 2: Assert narration LEVELS via a recording Logger spy</name>
  <files>src/runner.test.ts</files>
  <read_first>
    - src/runner.test.ts (the WHOLE file — the silent `Logger` fixture at lines 49-56, the `REASON` mirror at 189-196, the FakeApiClient-driven describe blocks)
    - src/runner.ts (the narration added in Task 1 — the call sites under test)
    - src/fake-api-client.ts (the scripted double's queue shape used to drive scenarios)
    - .planning/phases/04-logger-cli-live-smoke/04-PATTERNS.md (`src/runner.test.ts`-adjacent conventions; the "assert call shape, not rendered string" rule)
  </read_first>
  <action>
    Add focused tests to `src/runner.test.ts` that prove the LEVEL taxonomy without asserting
    on rendered strings. Replace (or supplement) the silent `Logger` fixture with a RECORDING
    spy that captures `{ level, message }` per call — e.g. a small factory returning a `Logger`
    whose four methods push `{ level: "info"|"warn"|..., message }` into a shared `calls[]`
    array (this stays offline; it implements the same `Logger` interface the silent fixture
    does). Use Vitest `vi.fn()` or plain push closures — Claude's discretion; keep it in-file,
    no new test helper module.

    Add at least these behavioral cases (drive each with `FakeApiClient` exactly as the existing
    happy-path test does):
      - Happy turn: a solvable board produces at least one `info` call for the chosen ad/outcome
        (assert an `info`-level call exists; assert on the LEVEL + that `message` is a non-empty
        string — NOT on exact wording, which is discretion).
      - Empty/no-eligible board: the `chooseAd === null` path produces a `warn` call (the
        nothing-to-do skip). Reuse the existing empty-board fixture style (`getMessages: [[]]`).
      - shoppingSuccess:false buy: a `getShop` returning an item the strategy picks, then a
        `buy` returning `shoppingSuccess:false`, produces a `warn` call. (Construct via the
        FakeApiClient `getShop`/`buy` queues; ensure the game still terminates so the test ends.)
    Assert LEVELS, not strings (PITFALLS TDD rule) so wording stays free to change. Keep every
    case offline (FakeApiClient only) — zero live network (TEST-01/D-12).
  </action>
  <verify>
    <automated>npx vitest run src/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/runner.test.ts` exits 0
    - A test asserts a `warn`-level call fires on the no-eligible-ad / empty-board path
    - A test asserts a `warn`-level call fires on a `shoppingSuccess:false` buy
    - A test asserts an `info`-level call fires for a normal solved turn
    - Tests assert on the recorded `level` (and that `message` is a non-empty string), NOT on exact log wording — `grep -c "toBe(\"INFO:" src/runner.test.ts` is `0` (no rendered-string assertions)
    - The full suite stays offline: `grep -cE "\\bfetch\\b|nock|msw|http(s)?://" src/runner.test.ts` is `0`
  </acceptance_criteria>
  <done>
    runner.test.ts proves the INFO/WARN level taxonomy on the chosen-ad, empty-board, and
    failed-buy paths using a recording spy Logger, all offline, asserting levels not strings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response fields → narration | Ad `message`, `probability`, encrypted text, and shop `name` from the live API flow into `logger.*` calls as fields |
| (none inbound) | Local CLI: no inbound network, no auth, no DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Log injection / forged log lines from untrusted ad/shop strings interpolated into narration | mitigate | Narration passes untrusted API strings as STRUCTURED object fields (`logger.info("chose ad", { adId, probability })`), never concatenated into the message text. `logger.ts`'s `foldArgs` keeps them as pino data so embedded newline/ANSI cannot forge log lines. The task explicitly forbids interpolating raw API strings into the message. |
| T-04-02 | Information Disclosure | Raw API arrays/objects (candidate ads, shop catalog) dumped above DEBUG | mitigate | Candidate/ranking/catalog/raw-object narration is confined to DEBUG (D-05/PITFALLS); INFO carries only scannable decision/outcome lines. No credentials exist in this game API (stated assumption), so DEBUG dumps disclose no secrets. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed this phase (RESEARCH: audit N/A). No install task in this plan. |
</threat_model>

<verification>
- `npx vitest run src/runner.test.ts` exits 0 (offline)
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `END` is exported; the three END strings and the playGame signature are unchanged
- Runner imports only the `Logger`/`ApiClient` interfaces (no console/pino/HttpApiClient)
</verification>

<success_criteria>
- Turn-level narration follows the INFO/WARN/DEBUG taxonomy (D-05/D-06), default run scannable
- END exported for DRY reason→exit-code mapping in index.ts (Plan 03)
- Loop mechanics, signature, guards, and END strings unchanged; Phase-3 tests still green
- New level-taxonomy tests pass offline, asserting levels not strings
</success_criteria>

<output>
Create `.planning/phases/04-logger-cli-live-smoke/04-02-SUMMARY.md` when done
</output>
